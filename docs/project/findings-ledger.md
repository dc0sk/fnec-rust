---
project: fnec-rust
doc: docs/project/findings-ledger.md
status: living
last_updated: 2026-08-29
---

# Findings ledger

Every discovery gets an ID here the moment it is found — not "to be reviewed
later". A finding that is noticed, discussed, and then quietly dropped is exactly
the thing this ledger exists to stop.

Before this file existed, discoveries scattered: one lived in a review document,
one in a changelog entry, one in a design doc, one only in a session recital of
"still open: X, Y, Z". Nothing was lost, but that was down to the recital rather
than to any structure.

## How to use it

- **ID**: `FND-NNN`, assigned in order, never reused. (`FIND-NNN` belongs to
  `docs/dev/reviews/review-260719.md` and is a different, closed series.)
- **Newest first.** New findings go at the top of the table.
- **State** is one of four, and only the first is non-terminal:

  | State | Meaning | Required |
  |:------|:--------|:---------|
  | `open` | found, not yet resolved | — |
  | `fixed` | resolved in the tree | the PR or commit that did it |
  | `deferred` | deliberately not now | an owner **and** where it is tracked |
  | `rejected` | will not act | a rationale |

- `scripts/check-findings-ledger.py` enforces the shape of every row: an ID, a
  known state, and the evidence that state requires. It runs in CI, so a
  `deferred` row with no owner or a `fixed` row with no reference fails the build
  rather than sitting here looking resolved.

An `open` row is not a failure — it is the point. What the process forbids is an
`open` row nobody can see.

## Ledger

| ID | Found | State | Finding | Evidence / owner |
|:---|:------|:------|:--------|:-----------------|
| FND-137 | 2026-08-30 | fixed | **[medium] The distributed sweep shuts the pool down BEFORE printing, and shutdown waits unboundedly, so a worker that wedges on the shutdown command hides a sweep that already succeeded.** `main.rs` calls `pool.shutdown_all()` above the output block, with a comment saying it is deliberate, and `shutdown` is an unbounded `child.wait()` with no kill (controller.rs, ssh_worker.rs). Every point can have been solved and every result computed, and the user sees nothing and gets no exit. | Found by fable while reviewing the FND-101/FND-102 design; the shape is FND-101's unbounded wait, at a site that finding names but that my three-part plan did not cover. The design doc already promises 'up to 2 seconds for graceful exit', which is prose only. Fix is that grace-then-kill, in the same change as the read deadline. — fixed in PR #439: shutdown gives the worker a 2-second grace period and then kills it, which is the figure the design doc already promised. Gated by a stub that ignores the shutdown command and never exits, so the test is bounded by the grace period or not at all. |
| FND-136 | 2026-08-30 | fixed | **[medium] The SSH path classifies a task-serialisation failure as a WORKER fault, so an unserialisable task evicts a healthy remote worker.** `controller.rs` maps `serde_json::to_string` failure to `DispatchError::Task` with a comment explaining why nothing has been sent; `ssh_worker.rs` maps it with a bare `to_string` that reaches `From<String> for DispatchError`, which classifies everything as `Worker`. Same decision, two copies, one diverged -- the house defect. | Found by fable while reviewing the FND-101/FND-102 design. WORSE THAN A MISCLASSIFICATION: it falsifies the doc comment I wrote on that `From` impl during FND-117, which asserts that every String error reaching a dispatch comes from the connect, reconnect or write path. It does not, and the counter-example is one file away. Fix belongs in the shared pipe-I/O core the FND-101 work extracts, not a spot patch, so the two handles stop being able to diverge here at all. — fixed in PR #439 by extraction rather than a spot patch: both handles now share one WorkerPipe, so a serialisation failure is classified once. It was a Task fault locally and a Worker fault over SSH, meaning an unserialisable task evicted a healthy remote host. It also falsified the doc comment I wrote on that From impl during FND-117; that comment now says Unreachable and is true because the shared pipe makes the alternative unrepresentable. |
| FND-135 | 2026-08-30 | open | **[low] A same-tag GM copy creates duplicate (tag, segment) pairs that fnec cannot address, where nec2c can.** With `ITGI = 0` and `NRPT >= 1` the copy keeps the original's tag, so two segments carry the same (tag, tag_index). Every EX/LD/NT/PT resolution in `excitation.rs` uses a `position` predicate matching tag and tag_index, which takes the FIRST match in definition order. nec2c addresses the copy by absolute occurrence -- `isegno(tag, mx)` -- so it can name 'tag 1 segment 77' on a 51-segment wire copied once; fnec can only ever reach the first 51. | Created by the FND-119 fix, which made same-tag copies representable for the first time, and left OPEN by an explicit decision recorded there: the nth-occurrence addressing rule is a separate mechanism touching six resolution sites, and folding it into that diff would have made it much harder to review. Agreement with nec2c is exact for indices within the first wire, so no currently-working deck is affected. The limitation is documented in the GM row of docs/card-support-matrix.md. |
| FND-134 | 2026-08-30 | fixed | **[low] Every `fixed` row opened in this remediation cycle cited a bare commit hash, and squash-merge made all of them unresolvable from `main`.** The ledger requires 'the PR or commit that did it' as evidence for a `fixed` row, and the established house style is the PR number (#430, #422, #417 and so on) precisely because a squash merge discards the branch commits. Verified: `git merge-base --is-ancestor` says NONE of 720bf3e, b40c192, e0036bc, 2503790, a6a5e12 or 02f3b63 is reachable from `main`. They survive today only because a local branch still points at them, and would become dangling on the next prune. | Mine, and self-inflicted. The rows were written before the branch was pushed, when no PR number existed yet -- which is exactly when the hash looks like the only available reference and is in fact the temporary one. Fixed in PR #434: all 22 citations now read `<hash> (PR #NNN, squashed to <main commit>)`, so the durable reference is present and the branch hash is kept for anyone reading the pre-squash history. The rule this earns: a `fixed` row written before its PR exists must be revisited when the PR merges, or cite the PR number from the start. `scripts/check-findings-ledger.py` accepts either form, so nothing mechanical catches it -- a checkable version would verify that every cited hash is an ancestor of `main`. |
| FND-133 | 2026-08-30 | open | **[medium] Two editor-pipeline completions still apply unconditionally: `EditDeckLoaded(Ok)` and `DeckSaved(Ok)`.** `EditDeckLoaded(Ok)` (app_state.rs) resets the undo history, so a stale load clobbers edits made since it was spawned, or delivers deck A's contents under path B. `DeckSaved(Ok)` calls `doc.mark_saved()` unconditionally, so editing during a slow save marks a dirty document clean. | Found by fable while reviewing the FND-115/FND-116 design, and deliberately scoped OUT of that fix: they are editor-pipeline rather than solver-pipeline, and neither finding names them. The mechanism to fix them now exists -- `RunId`, `claimed_run` and `accepted_run_for` -- so each is one arm in each of those two functions plus a pending id. Left open rather than swept in, to keep that diff reviewable. |
| FND-132 | 2026-08-30 | fixed | **[medium] Not one deck in `corpus/` routes to the conductor-path Hallen basis, so PH9-CHK-002 is exercised only by in-source unit tests.** Probed every `corpus/*.nec` through `hallen_route`: ZERO have `paths == true`. The basis needs a start-to-start or end-to-end REVERSED split (the `SPLIT_V` shape in hallen_session's tests); an ordinary end-to-start bend reduces to trivial paths and takes the plain basis. | Found while measuring FND-120, and it cost me a wrong conclusion first: I built what I thought was a bent deck, measured it, and reported that the path sibling shared the defect. It did not -- `hallen_route` returned `paths=false` and I had been exercising the plain builder twice. WHY THIS MATTERS BEYOND COVERAGE: FND-121 was a CRITICAL finding about this exact basis being wired to only one of four frontends, and it was found by an audit rather than by the corpus, because the corpus cannot reach the code at all. A regression that re-broke the path basis would pass every corpus gate. Fix is a corpus deck of the SPLIT_V shape with a reference row, plus one for the current-source and receive variants of the same routing decision. — fixed in PR #436: corpus/split-v-conductor-path-freesp.nec now reaches the basis, with a reference row and TWO gates, both sabotage-verified. The fixture asserts it takes the path basis, and a STANDING check asserts the corpus still contains something that does -- so the gap cannot be re-opened by deleting one deck, which pinning only the fixture would have allowed. MY CHARACTERISATION WAS TOO NARROW AND SABOTAGE CAUGHT IT. I wrote that the basis needs a start-to-start or end-to-end split and that an ordinary bend takes the plain basis; reversing one wire in the sabotage still routed. Measured across all four ways two wires can meet: start-to-start, end-to-end and start-of-first-meets-end-of-second all route here, and ONLY 'end of 1 meets start of 2' -- wires listed in the order you would walk them -- reduces to trivial paths. That single exception is what people write by hand, which is much of why no deck reached this code. External tolerances are wide deliberately (nec2c 268.56 +j452.26 against fnec 264.882 +j410.856, R within 1.4 percent and X within 9.2) and are the documented Hallen systematic, not a defect being hidden; the tight gates are the self-pinned regression. |
| FND-131 | 2026-08-30 | fixed | **[low] `docs/card-support-matrix.md` claimed GM support was 'Full', which is false under any reading of the card.** The legend defines Full as 'complete implementation', and its Notes column described fnec's own non-standard field meanings as though they were the card's. NRPT is not implemented at all. | Found by fable while reviewing the FND-119 design. Corrected to `Partial` with the actual divergence named and a pointer to FND-119, in the same change. Fixed in ca03654 (PR #432, squashed to 4e7170f). Worth its own ID rather than being folded into FND-119 because the defect class is different: FND-119 is wrong behaviour, this is a capability table asserting a completeness the code never had, which is what people design against. |
| FND-130 | 2026-08-29 | open | **[low] The entire pulse current-source apparatus is unreachable, and a corpus row carries a reference impedance that can never be produced.** An EX 4 deck with any non-Hallen solver is refused at `solve_session.rs:1027` before the pulse machinery runs, so `PulseCurrentSourceConstraint`, `collect_pulse_current_source_constraints`, `apply_pulse_current_source_constraints` and `pulse_current_source_voltage` are all dead. | VERIFIED, not inferred: `--solver pulse` on corpus/dipole-ex4-pulse-current-freesp-51seg.nec gives EXIT 1 and 'EX: current-source excitation requires --solver hallen'. That corpus row nevertheless still carries `feedpoint_impedance` -345.626023 -j988.019173, a number the deck can no longer produce -- its `status` correctly says 'fails fast', so the stale figure is unread rather than wrong, but it reads as a live reference. This is DORMANT, not orphaned: the refusal is a recorded decision, so per the repo's discipline the code should be retired dormant with a tag and a retention rationale rather than deleted, and the stale impedance removed from the row. Found by fable while reviewing the FND-114 fix; the dormant fallback is retained at the new shared seam rather than dropped silently. |
| FND-129 | 2026-08-29 | fixed | **[high] Two current sources on different tags: only the first is driven, and the second's impedance is computed from the first source's port voltage over a current nobody impressed.** `first_current_source` (hallen_session.rs:101) and the `find_map` in `solve_current_source_hallen` (current_source.rs:98) both take the FIRST EX 4 and ignore the rest, while `mixed_excitation_error` (validate.rs:917) refuses only voltage-plus-current, so a second current source passes validation in silence. The CLI then builds a feedpoint row per EX card and prices every one of them from the single `current_source_port`. | MEASURED on a two-wire deck with `EX 4 1 11` and `EX 4 2 11`, both asking for 1 A. EXIT 0, ZERO warnings. Row 1: V = 42.004192 -j121.198169, I = 1.000000, Z = 42.00 -j121.20. Row 2: V IDENTICAL at 42.004192 -j121.198169 (the FIRST source's port voltage) but I = -0.250051 -j0.311050, which is not 1 A at all -- it is induced current from the other element. Its reported Z of 170.74 +j272.30 is therefore a wrong voltage over a current that source never drove. The non-unity current is the decisive part: it proves the second source was never impressed, rather than merely mis-reported. Sibling of FND-120 (second delta-gap on the SAME tag dropped); this is a different card type, different tags, different mechanism. The honest interim fix is a refusal in `validate` on the FND-036 pattern, not API growth. Found by fable while reviewing the FND-114 fix and reproduced by me. — fixed in 02f3b63 (PR #432, squashed to 4e7170f): `multiple_current_sources_error` refuses the deck from `pre_solve_error`, the one gate all four frontends reach, on the FND-036 precedent. Supporting N sources is real solver work (N pinned constraints, N port voltages, where CurrentSourceFeedpoint carries one of each) and no corpus deck carries two, so nothing is lost by declining to print numbers that were never meaningful. Deliberately NOT extended to delta gaps: fnec superposes voltage sources across tags and that path works, so counting drives rather than current sources would refuse decks that solve correctly today; the same-tag delta-gap defect stays visible as FND-120. ONE SEAM, FOUR GATES. Each frontend gets its own test that the seam is reached, because a unit test on the refusal stays green for all four when the wiring is dropped -- verified by sabotage: removing the single line from `pre_solve_error` leaves the unit test passing and fails the CLI, GUI, worker and bindings gates together. The GUI is gated on BOTH its entry points, and the bindings reach the refusal through `diagnose` rather than `pre_solve_error` directly, which is a second route and so a second place the wiring can be missing. New guardrail deck corpus/dipole-two-current-sources-freesp.nec. Two GATE defects were found on the way and fixed in b20301e (PR #432, squashed to 4e7170f): nothing ran the bindings crate's Rust tests at all (outside the workspace, and clippy only compiles them), and the FND-117 stub worker raced on fork/exec (ETXTBSY from a sibling test inheriting its writable fd) -- now isolated in its own test binary, three consecutive full-gate runs green. |
| FND-128 | 2026-08-29 | open | **[medium] The conductor-path routing decision FND-121 centralised survives in a third copy, in the CLI's receive-pattern sweep.** `solve_plane_wave_hallen` (apps/nec-cli/src/solve_session.rs:290) re-derives `build_conductor_paths(segs)` then `paths.iter().any(not is_trivial)` inline, which is exactly what `nec_solver::hallen_session::nontrivial_paths` (crates/nec_solver/src/hallen_session.rs:130) now exists to be the single copy of. | NOT YET DIVERGED, and recorded for that reason. The two predicates are today identical in effect, verified by reading both. This is the setup for the repo's dominant defect class rather than an instance of it: FND-121 was the same decision in four copies with one diverged, and consolidating three of them while leaving a fourth inline is how the next divergence gets built. Found by fable during the design review of the FND-113/117/125/126 fix set. |
| FND-127 | 2026-08-29 | fixed | **[high] The CLI receive-pattern sweep launders NaN currents into a plausible-looking number instead of reporting them.** `plane_wave_receive_sweep` reduces each incidence angle to `currents.iter().map(norm).fold(0.0f64, f64::max)` (apps/nec-cli/src/solve_session.rs:372, and again at :375 for the normalisation). Rust's `f64::max` returns the OTHER operand when one side is NaN, so a fully diverged solve folds to exactly 0.0 and prints as a -999.99 dB null in the receive pattern -- indistinguishable from a genuine radiation null. | This is strictly worse than FND-126's symptom, which at least prints the word NaN where a reader can see it. Same root class -- a solve that did not converge is reported as an answer -- but the opposite failure mode, so it needs its own fix rather than sharing FND-126's guard alone. Found by fable during the design review of the FND-113/117/125/126 fix set; `f64::max` NaN semantics confirmed against the standard-library contract. — fixed in 1bfdf79 (PR #432, squashed to 4e7170f) together with FND-126: `check_currents_finite` now runs BEFORE the folding reduction, so the laundering has nothing left to launder. Verified end to end, a diverged receive pattern exits 1 instead of printing -999.99 dB, and sabotage-verified, removing the guard makes the test fail while the healthy-deck control stays green. |
| FND-126 | 2026-08-29 | fixed | **[high] The non-finite-current guard sits on the feedpoint path only, so a diverged plane-wave receive solve prints a full block of NaN currents and exits 0.** `FeedpointError::NonFiniteCurrent` is raised only inside `feedpoint_impedance` (crates/nec_solver/src/feedpoint.rs:86), which a receive deck never reaches because it has no feedpoint. The result is a blind sibling path: the guard works on the driven path and does not exist on the receive path. | DECISIVE ABLATION, one deck, one card changed. corpus/dipole-ex1-freesp-51seg.nec with the wire radius set to 0.0 gives EXIT 0, a full report, 51 rows of `1 n NaN NaN NaN NaN` in the CURRENTS block, 52 NaN occurrences and ZERO warnings. The same radius on the otherwise identical DRIVEN deck gives EXIT 1 and the correct diagnostic, quote, the solved current at the feedpoint is not a finite number, so the solve did not converge. The trigger is not exotic: a zero radius is a plausible deck typo and is not refused at parse or validate time. PROVENANCE, and why this is not already covered: the shape was measured during the 2026-08-28 audit and written into FND-076's evidence, but FND-076 was closed on the frequency-usability gate alone (0460377 (PR #432, squashed to 4e7170f)), which does NOT touch this path. That gate does now refuse the ORIGINAL repro (a NaN sweep-config frequency is rejected, verified), so this row records a live defect that survived inside a row marked fixed, reached by a different input. — fixed in 1bfdf79 (PR #432, squashed to 4e7170f): one shared `check_currents_finite` in nec_solver, called at four sites. The design review established there is no single choke point, the CLI's GPU-resident arm and its receive-pattern sweep exist precisely BECAUSE they bypass the session entry points, so one implementation at four call sites is the honest shape rather than one seam. Sites 1 (solve_hallen_routed, wrapped so its three return arms share one guarded exit), 2 (solve_mpie_session, which had no finiteness check at all) and 4 (solve_plane_wave_hallen) are demonstrated by runs. SITE 3, the CLI GPU-resident arm, is wired by reading only, this host reports gpu_available=false so that arm cannot be entered here. Sabotage-verified on the two reachable defect tests. |
| FND-125 | 2026-08-29 | fixed | **[high] GR and GM geometry replication have no bound on how much they allocate: a deck-supplied copy count is trusted and expanded before anything checks it.** `apply_gr` (crates/nec_solver/src/geometry.rs:797) loops `for copy_idx in 1..=gr.count` pushing `originals.len()` segments per iteration; `gr.count` is a u32 read straight from the card (crates/nec_parser/src/lib.rs:164). There is no segment cap anywhere in nec_solver or nec_parser; the only cap in the crate is MAX_FR_POINTS. Same shape as FND-109, which was fixed by refusing before expanding. | MEASURED SAFELY under `ulimit -v 2000000`: a ONE-segment deck with `GR 1 100000000 10.0` aborts with SIGABRT, exit 134, core dumped, and the message `memory allocation of 2147483648 bytes failed` -- it was still growing when it hit the 2 GB ceiling, so the loop is genuinely unbounded, not merely large. No refusal, no warning, no cap on the path. Aggravates FND-117: an aborting worker emits no JSON line, so this reaches the pool as a dead worker. Found while fixing FND-113, which is the unchecked tag arithmetic in the same two functions. — fixed in e2e8f1d (PR #432, squashed to 4e7170f): MAX_SEGMENTS = 10 000, checked as a RUNNING total in u64 before each card expands, at all three call sites including `expand_wire`. The design review corrected the design on all three points, the largest hole needed no GM or GR at all (`GW 1 4000000000` is a ~512 GB allocation from a one-line deck, which my first design would have missed entirely), a per-card limit cannot see fifteen composing GM doublings, and a u32 projection wraps into a passing number for exactly the inputs being guarded. Cap derived from the matrix rather than the segment list, ZMatrix is 16*N^2 bytes and O(N^3) to factor, so 10 000 is already ~1.6 GB. Corpus max is 255 segments, asserted as a negative control. At the worker boundary this becomes ErrorCode::ResourceExhausted, which had sat in the protocol enum with zero producers since the crate's first commit. Sabotage-verified twice, guards and wire mapping. |
| FND-124 | 2026-08-29 | open | **[medium] `LD` loads on the pulse, continuity and sinusoidal bases still use the undrived diagonal stamp.** FND-122 derived the load treatment for the Hallen basis only. Pulse and continuity solve a Pocklington system in field units; sinusoidal uses the Hallen matrix with a basis in which a rank-1 column update is not the load-current sample. All three keep the old ohms-on-the-diagonal add. Sinusoidal matters most: `warnings.rs` names it as the accurate alternative to the experimental pulse/continuity modes, so a user steered there for accuracy gets the unvalidated load model. | Found while fixing FND-122; flagged in review by fable, which established that the single derivation does not transfer to those bases. Now caveated at every frontend rather than silent (e0036bc (PR #432, squashed to 4e7170f)) — the warning is not the fix. Needs either a per-basis derivation or an explicit refusal. |
| FND-123 | 2026-08-29 | open | **[medium] `TL` and `NT` two-port couplings carry FND-122's units error and are stamped as series impedances into the dimensionless Hallen matrix.** Not fixable by the same rank-1 column: a two-port couples port *voltages*, which are not unknowns in the Hallen system, so a correct treatment needs additional unknowns and equations rather than a different stamp. Compounded by FND-081: a standard NEC-2 `TL` card is misread by fnec's field layout, and one with nonzero shunt admittances is accepted with no warning at all and silently models a 0.02 ohm, 1 cm line where the deck asked for 50 ohm, 1.5 m. | Caveated at every frontend in e0036bc (PR #432, squashed to 4e7170f) rather than answered silently. THE OPEN DECISION IS REFUSE-OR-DERIVE, and it is the developer's: refusing is the recommended end state (it is what the MPIE already does) but it changes which decks fnec accepts, and roughly 7 corpus rows are pinned on current TL/NT behaviour. There is also an asymmetry to settle — malformed NT cards are warn-and-skip today, so refusing well-formed ones would mean fixing your card stops your deck solving. |
| FND-122 | 2026-08-28 | fixed | **[critical] LD/TL/NT/Laplace load stamps (ohms) are added to the dimensionless Hallén A-matrix — every loaded deck solves to wrong physics on all four frontends** | Found by the 2026-08-28 whole-project audit (R56); confirmed by adversarial verification. HELD AT CRITICAL. Ohms confirmed added raw to a dimensionless matrix: loads.rs:95 documents output as Ohm, matrix.rs:1-22 A[m,n]=cos(alpha)*int G dl, RHS carries 2*pi/ETA0. PORT IDENTITY MEASURED: 100 ohm at feed -> fnec +699.86+j134.76 vs nec2c 79.348+j46.223 -> 179.35+j46.223, identity holding to the printed digit. Over-application is a clean LINEAR 7.0x (0.1->+0.700, 1->+6.999, 10->+69.99); fnec's own diag_spread=10.375 implies a 100-ohm stamp is ~9x the entry it lands on. Off-feed 50-ohm SERIES RESISTORS make R go DOWN (-2.29-j0.39) where nec2c gives +70.8. LD 5 copper 20x understated. No warning on any run. SELF-CONSISTENT CHECKER CONFIRMED IN-TREE: corpus/reference-results.json 'dipole-ld-loaded-51seg' records fnec 7422.77+j1428.85, an external nec2c candidate of 1129.3+j46.223, ExternalR_absolute_ohm tolerance WIDENED TO 6500.0, and status 'External nec2c reference not yet alig... Detail: `docs/dev/reviews/review-260828.md`. — fixed in e0036bc (PR #432, squashed to 4e7170f): loads now enter as a derived rank-1 column (-j*2pi/eta0 * Z_p * sin(k*abs(s_m - s_p))) in the basis that runs, instead of ohms on a dimensionless diagonal. Port identity holds to 0.007 ohm; off-feed and LD 5 cases match nec2c within the Hallen systematic and match an independent replica to 4 s.f. Three self-pinned corpus rows and three CLI tests re-derived against analytic values; the external tolerance on dipole-ld-loaded-51seg drops from 6500 to 10.1 ohm. Sabotage-verified. TL/NT keep the same units error and are now caveated rather than silent — refusing them is a separate decision, see FND-123. |
| FND-121 | 2026-08-28 | fixed | **[critical] The PH9-CHK-002 conductor-path Hallén solve is CLI-only: GUI, fnec_py and the worker silently answer bent/split geometry with the pre-paths solve, and the shared junction caveat is suppressed for exactly that class** | Found by the 2026-08-28 whole-project audit (R43); confirmed by adversarial verification. HELD AT CRITICAL, nothing downgraded. Verifier re-measured: CLI 264.882+j410.856; worker same deck/Hz 9.152-j767.601 status ok warnings []; nec2c oracle 268.56+j452.26. Worker wrong by 29x in R with the reactance sign flipped. Confirmed the suppression is genuinely shared: BOTH warning producers consumed by the other frontends (frequency_independent_caveats validate.rs:1024 and diagnose's Hallen arm validate.rs:1168) call feedpoint_at_junction_warnings, AND classify_unsupported_topology returns None whenever build_conductor_paths is Some -- so every caveat channel is closed for exactly the class solved wrongly, and R stays positive so the negative-R tripwire is silent too. No non-default config, no test seam: Hallen is the default and bent/split geometry is among the most common ham decks. Only correction: GUI and fnec_py divergence is established structurally (identical else-branch, ... Detail: `docs/dev/reviews/review-260828.md`. — fixed in 2503790 (PR #432, squashed to 4e7170f): routing moved to nec_solver::hallen_route / solve_hallen_routed, all four frontends migrated, GPU gate and caveat suppression corrected; sabotage-verified against the measured 29x divergence. |
| FND-120 | 2026-08-28 | fixed | **[high] Second delta-gap source on the same wire tag is silently dropped — a symmetric two-source dipole yields asymmetric currents and two different feed impedances** | Found by the 2026-08-28 whole-project audit (R58); confirmed by adversarial verification. HELD. Reproduced asymmetry on a mirror-symmetric deck: Z(seg16)=81.50-j122.79 vs Z(seg36)=105.94-j130.10, no warning. DECISIVE ABLATION: deleting the second EX card gives a CURRENTS block BYTE-IDENTICAL to the two-source run (diff empty), proving the second source contributes exactly nothing -- while the report still prints N_SOURCES 2 and a FEEDPOINTS row for (1,36) at V=1.0 with an impedance derived from a current that source never drove. Sibling build_hallen_rhs_paths superposes over every EX with no tag dedup. All three corpus multi-source decks put their EX cards on different tags. CORRECTION: the finder's quoted numbers came from an unspecified deck and did not reproduce; the defect did. Detail: `docs/dev/reviews/review-260828.md`. — fixed in 720bf3e (PR #433, squashed to 27c1d22): the sources are collected per CARD, in deck order, instead of into a BTreeMap keyed by wire tag. The superposition below was always written for several sources; the collection was what deduplicated. This converges the plain basis onto `build_hallen_rhs_paths`, which iterates the cards directly and was always right. RE-MEASURED SOUNDLY: the original ablation used `diff`, which is NOT INSTALLED on this host and silently reports equality -- see the note on FND-114. By md5, the two-source and one-source CURRENTS blocks were genuinely byte-identical before the fix and differ after. Feedpoints on a mirror-symmetric dipole fed at segments 16 and 36 went from 112.181884 +j16.627983 versus 106.722105 +j28.781021 to 54.885218 +j11.470780 at BOTH, equal to the digit. Gated at two levels, both sabotage-verified, on a symmetry property that cannot pass for the wrong reason: it holds whatever the basis does, and dropping both sources fails a separate 'actually driven' assertion. Two controls guard it (a single off-centre gap must stay asymmetric; a missing segment must still error). Also fixed here: a later EX naming a nonexistent segment was silently skipped while the first card's was a hard error and every card's is one in the path sibling. Now an error for all -- the FND-026 shape. Corpus multi-source decks are unaffected, since all three put their EX cards on different tags. That is also why the corpus never caught this, and why FND-132 is now open. |
| FND-119 | 2026-08-28 | fixed | **[high] GM card field 2 parsed as 'last tag' where NEC-2 defines it as NRPT (number of new structures) — standard GM decks silently lose wires** | Found by the 2026-08-28 whole-project audit (R57); confirmed by adversarial verification. HELD. Settled against nec2c. Two-GW deck + GM: nec2c 44 segments tags 1/2/11/12, fnec 33 segments tags 1/2/11 -- wire 2 silently not copied, no warning. NRPT>=2 unrepresentable: nec2c 33 segs tags 1/11/21, fnec 22 segs tags 1/11. Short-field claim holds (fnec exits 1 where nec2c accepts trailing blanks). CORRECTION THAT STRENGTHENS THE FINDING: the second corpus GM deck is NOT a coincidence fixture -- it already diverges. nec2c on dipole-freesp-gm-inplace-shifted.nec gives 102 segments (NRPT=1 with ITGI=0 makes a same-tag copy) where fnec gives 51. So fnec's 'ITGI==0 => move in place' rule is ALSO non-NEC: in NEC it is NRPT==0, not ITGI==0, that suppresses replication. Detail: `docs/dev/reviews/review-260828.md`. DESIGN REVIEW 2026-08-30 SETTLED THE SEMANTICS AGAINST nec2c AND CORRECTED ME ON TWO POINTS. I2 = NRPT and the per-copy transform is CUMULATIVE (copy k = transform applied k times, tag + k*ITGI), both confirmed: `GW 1 3` + `GM 10 2 0 0 0 0 0 1. 0` gives nec2c 9 segments tagged 1/11/21 at z-centres -1/3,0,1/3 / 2/3,1,4/3 / 5/3,2,7/3, against fnec's 6 segments tagged 1/11. But F7/ITS is NEITHER a tag range NOR 'all tags >= ITS': it is the SUFFIX of the structure from the first segment carrying that tag onward IN DEFINITION ORDER. Proven with wires defined tag 5, tag 2, tag 3 and `GM 0 0 0 0 0 0 0 10. 2.`, where nec2c moves wires 2 AND 3 and leaves 5 alone, which neither of my two candidate readings predicts. THREE FURTHER SUB-DEFECTS not previously recorded: (1) an in-place move (NRPT=0) must ALSO apply ITGI to the tags -- nec2c retags a tag-1 wire to tag 11 -- except that tag-0 segments are never retagged; (2) an ITS naming a tag no wire carries is a HARD ERROR in nec2c (exit 255) where fnec silently no-ops and solves at exit 0; (3) nec2c accepts a NEGATIVE ITGI, which fnec's u32 cannot represent. Also: the existing short-field refusal is real (`GM 0 1 0 0 0 1.` is accepted by nec2c and refused by fnec at exit 1) and belongs in the same change, since the reparse must define per-field defaults anyway. ADJACENT HAZARD the fix creates: same-tag copies give duplicate (tag, tag_index) pairs, and EX/LD/NT/PT resolution takes the first match, so nec2c can address the copy as 'tag 1 seg 77' and fnec cannot -- decide between the mx-th-occurrence rule and a documented limitation rather than leaving it open. THE DECISION IS THE DEVELOPER'S: adopting NEC semantics changes what shipped decks MEAN in a released product, and the current subset is documented as a deliberate choice in corpus/README.md and docs/nec4-support.md. Recommendation is to adopt NEC semantics outright with no compatibility mode, since the present field meanings can only appear in decks already broken on every other engine. The false 'Full' claim in the support matrix is now FND-131. DECISION TAKEN 2026-08-30 by the developer: adopt NEC-2 semantics outright, no compatibility mode (option A of four put to them). The tag-range capability those fields used to carry is NOT discarded silently -- it is recorded as requirement FR-011, `status = planned`, which states that if it returns it gets its own syntax and never a standard field. Occupying a standard field is what caused this finding, and the failure is silent in both directions. Two sub-decisions settled with it: the nth-occurrence addressing rule for duplicate (tag, segment) pairs is documented and ledgered separately rather than folded in, and short-card acceptance IS folded in, since the reparse must define per-field defaults anyway. — fixed in PR #435 (option A). All five NEC-2 rules implemented and each sabotage-verified SEPARATELY against nec2c captures of 2026-08-30: NRPT decides copy-versus-in-place, copies are cumulative and tagged tag + k*ITGI, an in-place move retags too, tag-0 segments never retag, and ITS selects a suffix in definition order with an absent tag refused. Short cards accepted and a negative ITGI refused by name through a new ParseError::UnsupportedValue, since nec2c yields tag -9 there and `as u32` would have saturated it to 0 and silently made a same-tag copy. THE TAG-0 RULE HAD NO TEST UNTIL SABOTAGE FOUND IT -- removing the exemption changed nothing, because the other six cases cannot see it. That is the second time this cycle that sabotaging a rule separately caught an untested one. Corpus: dipole-freesp-gm-inplace-shifted.nec RE-AUTHORED rather than re-pinned, because its card meant a same-tag copy while its README entry claimed in-place invariance; as `GM 0 0 ... 0` it tests what it always said and keeps 74.242874 + j13.899516 exactly. multi-source-gm-copy.nec is untouched and still passes -- at NRPT=1 the old and new readings agree, which is precisely why the corpus never caught this, and dipole-gm-nrpt2-freesp.nec now closes that gap. The evicted tag-range capability is requirement FR-011; the addressing consequence is FND-135. GM moves from PARTIAL to FULL in the support matrix and nec4-support. |
| FND-118 | 2026-08-28 | fixed | **[high] Current-source (EX 4) and voltage (EX 0) drives report different impedances for the same antenna over ground — every EX4 corpus fixture is free-space, so the class is ungated** | Found by the 2026-08-28 whole-project audit (R50); confirmed by adversarial verification. HELD. Reproduced all three numbers exactly. GN 1 emits NO warning at all; GN 2 emits only the generic sub-0.1-lambda warning that fires identically for EX 0. Mechanism substantiated: the EX-4 RHS synthesizes a V=1 delta gap through the free-space Hallen sin/cos forms and least-squares-solves against the ground-modified z_mat; the free-space homogeneous basis stops fitting once the image kernel enters. VIOLATES THE PROJECT'S OWN STATED GATE, quoted from linear.rs: 'V/i0 equals the voltage-source feedpoint impedance -- the internal consistency gate used to validate this path.' Every EX-4 deck in the corpus and in every in-source test is GE 0 free space, so the gate is asserted only where it cannot fail. CORRECTIONS: the honest range is ~2-14%, height-dependent, not 6-14% -- at 0.24 lambda (no warnings at all) GN1 still splits 5.3%. And MPIE is NOT a clean tie-breaker: R sits near the EX... Detail: `docs/dev/reviews/review-260828.md`. SECOND MANIFESTATION, measured 2026-08-29 while fixing FND-114: the disagreement is not confined to impedance. On the GN 2 near-ground dipole the same antenna gives R 92.266 under EX 0 and R 86.238 under EX 4 (6.5%), and the PATTERNS differ by +0.233 to +0.260 dB at every above-horizon point. Cause identified in review: the current-source solve pins end currents hard (cw 1e6) where the delta-gap solve leaves them soft (measured end currents ~2e-4 vs ~1e-13), and over finite ground that formulation difference costs ~6.5% in port voltage; recomputing the far-field integral from the two printed current distributions gives a ratio of 1.061 = +0.258 dB, matching the row deltas. Now frozen and documented by corpus/dipole-ex4-gn2-near-ground-51seg.nec, so the class is no longer ungated even though it is not yet fixed. — fixed in PR #437. A current source is now the unit-voltage solve rescaled by i0 divided by I_feed, exact for a linear system, so the two drives agree BY CONSTRUCTION on any geometry over any ground. 215 lines of bespoke solver deleted. MY DIAGNOSIS WAS WRONG AND THE ABLATION REFUTED IT. I attributed the split to a diverged constraint weight (1e6 in the current-source solver against 1.0 in the delta-gap one); sweeping it across NINE orders of magnitude moves R by 0.28 percent against a 6.5 percent gap, and equalising both drives leaves the split intact. The end-current difference I had measured was correctly attributed to the weight, but inferring the impedance difference from it was the unfalsified step. REAL CAUSE: over finite ground the augmented Hallen system becomes INCONSISTENT -- the free-space homogeneous basis does not satisfy the ground-modified operator, residual rising from 3e-7 to 5.8 percent -- so the port voltage is pinned only to about 4 percent and the two drives minimised different objectives over a nearly flat valley. Exact constraint elimination was also refuted: cw=1e9 is numerically identical to 1e6, so the weighting already sits at that limit. WHICH DRIVE TO KEEP was settled by an oracle that is neither. nec2c cannot arbitrate, because its EX 4 is an elementary Hertzian source at a POINT, a different drive from fnec's segment current. MPIE, a different kernel with a different ground model, gives R = 91.208: the voltage drive is 1.2 percent away, the old current drive 5.4 percent. THE FIX WAS HALF DONE WHEN FIRST MEASURED: hallen_session intercepts the conductor-path case with its own copy, so fixing current_source.rs alone left junctioned decks on the old solver. Caught by measuring a paths-routed deck rather than assuming -- a start-to-start split gave 247.935 +j384.841 against 264.882 +j410.856, 6.4 percent apart IN FREE SPACE. The defect was never confined to ground; it appears wherever the augmented system is inconsistent. GATE: 'the two drives agree' is now definitional and can never fail, so the gate is anchored on MPIE instead -- 5.45 percent apart before and failing, 1.16 percent after and passing. Reactance deliberately not gated against MPIE, since that gap is the drive-INDEPENDENT reflection-coefficient systematic and EX 0 would fail it too. My first sabotage of the gate was INERT (scaling currents and port voltage together cancels in V over I); perturbing only the port voltage reproduces 5.42 percent. |
| FND-117 | 2026-08-28 | fixed | **[high] An infinite VSWR crosses the worker wire as JSON null, which the controller cannot parse — one negative-R or purely reactive point evicts every worker in the pool** | Found by the 2026-08-28 whole-project audit (R44); confirmed by adversarial verification. HELD. Whole chain read and two links measured. Worker emitted vswr_50:null for corpus/inverted-v-negative-r-freesp.nec (Re Z = -5.973). A throwaway crate confirmed serde returns Err('invalid type: null, expected f64') and that INFINITY serializes to null. Both transports map that to a dispatch Err. Cascade real in BOTH dispatch paths, not just the cited one: pool.rs:161-167 loops removing every erroring worker, ending 'all workers in pool failed'. Aggravator: main.rs:339 REFUSES --solver mpie with --hosts, so junctioned decks -- exactly the ones Hallen drives to Re(Z)<=0 -- are forced down the failing path. No test covers a null or non-finite vswr. Detail: `docs/dev/reviews/review-260828.md`. — fixed in cdb636d (PR #432, squashed to 4e7170f), two independent defects. WIRE, the format is unchanged (null is what already crossed), only the READER is taught to accept it, so both directions of version skew keep working. Two companion changes the design review required to make 'null means infinite' true by construction rather than by luck: `vswr()` maps its one NaN-producing case (both sums overflowing for an astronomical Z, which IS an open circuit) to infinity, and a NON-FINITE IMPEDANCE becomes a task error before a line is built -- without that the serde helper is INERT for that case, since Impedance is two plain f64s in the same struct. POOL, DispatchError::Worker vs Task. A result that fails to deserialise is a task fault because the protocol is line-framed and the parse happens after a complete line is read, so the stream is in sync. Applied to dispatch AND dispatch_batch. EVIDENCE NOTE, the two halves mask each other: the end-to-end negative-resistance test passes with the eviction bug fully restored, measured under sabotage, because once the VSWR round-trips that deck produces no task fault at all. The taxonomy is discriminated instead by a stub worker answering with a complete in-sync line that is not a usable result. Both halves sabotage-verified independently. |
| FND-116 | 2026-08-28 | fixed | **[high] All 3-D viewport completion arms (GeometryLoaded, CurrentsSolved, Pattern3dComplete) are completely unguarded — stale solves repopulate the viewport and re-enable overlays after the state that launched them is gone** | Found by the 2026-08-28 whole-project audit (R30); confirmed by adversarial verification. HELD. ViewportState has no phase enum and no run id; all three completion arms apply unconditionally while the four tabbed pipelines are each guarded. discard_solver_derived_state clears exactly the fields these arms unconditionally rewrite. Solver captured at spawn time, so a completion genuinely carries the other solver's numbers; the solver pick_list is always visible beside the viewport and the buttons are never disabled in flight. CORRECTION: the GeometryLoaded leg is weakest (geometry is not solver-derived and it does null currents_ma). Display-only, but it silently undoes a deliberate guard and switches on overlays the user never touched. Detail: `docs/dev/reviews/review-260828.md`. — fixed in b40c192 (PR #433, squashed to 27c1d22) with FND-115. The three viewport arms get three SEPARATE identities, not one: geometry is deck-derived and a load in flight across a solver switch must still land, so a blanket orphan-everything discard would wrongly drop it -- sabotaging that asymmetry fails exactly one test, and sabotaging the guard fails eight. Two corrections from the design review that the row did not have: there is a SECOND discard site, `refresh_editor_preview`, where a stale completion undid an editor edit's clearing just as it did a solver switch's; and the Err arms needed the guard as much as the Ok arms, since a stale geometry FAILURE blanked a good scene unconditionally. `EditDeckLoaded` and `DeckSaved` have the same shape and are deliberately left open as FND-133. |
| FND-115 | 2026-08-28 | fixed | **[high] Boolean phase guards cannot tell WHICH in-flight request a completion belongs to; with no task cancellation, a superseded solve/sweep wins and the correct result is dropped** | Found by the 2026-08-28 whole-project audit (R29); confirmed by adversarial verification. HELD. awaiting_solve/awaiting_sweep are kind-of-run booleans with no run identity; no abort/cancel anywhere in nec-gui; discard_solver_derived_state resets phase but leaves the task running, and can_solve/can_sweep re-enable immediately. Sweep leg traced concretely: A's next point hits the Running arm and seeds the accumulator with an old-solver point, both streams then append into one Streaming vec, A's SweepStreamDone finalizes the mixture as Done. CORRECTION: leg 1 does not need the ungated Apply+Solve button -- the gated Solve button alone reaches it. Existing regression tests cover only the Idle window. Detail: `docs/dev/reviews/review-260828.md`. — fixed in b40c192 (PR #433, squashed to 27c1d22), together with FND-116: they are one defect. A completion carried no identity, so the reducer could not tell a run it launched from one it had disowned. `RunId` is minted only by `AppState::mint_run` and its inner value is PRIVATE, so neither the binary nor a test can fabricate a stamp -- a test that hardcoded an id would not compile, which makes 'this test used a real run' compiler-checked. The id lives IN the phase rather than in a field beside it, so a discard orphans in-flight work structurally and there is no bump to forget. ONE guard before the match, not eleven per-arm ifs: deleting that single seam fails all eight stale-rejection tests at once, verified by sabotage. Scope is every pipeline that spawns work, not the two the finding names: the pattern and currents TABS had the identical kind-only guard and `DeckWarnings` had no phase at all. The same-solver window is pinned separately, because Apply+Solve is a second arming site with an ungated button, so a fix keyed on solver identity would pass every solver-switch test and still lose a result. |
| FND-114 | 2026-08-28 | fixed | **[high] Gain-correction input power diverges between CLI and GUI on current-source decks: feedpoint_input_power reads the all-zero excitation vector, so the GUI silently skips the lossy-ground correction** | Found by the 2026-08-28 whole-project audit (R24); confirmed by adversarial verification. HELD. feedpoint_input_power uses v_vec[idx]*length, but apply_ex early-returns for CurrentSource/PlaneWave without writing v, so p_in = 0.0 exactly -> gain_correction_db returns None -> gui_gain_correction_db unwrap_or(0.0). The fix is available on that very path: solve_currents returns Some(fp.port_voltage) and it is discarded at solve.rs:1072 `let (currents, _port_voltage)`. CLI is the correct sibling and never calls the shared producer. CORRECTION: the real delta is vs the CLI's EX-4 number (-27.2174), so ~5.78 dB, not 6.04. GUI value taken from the finder; the SKIP is proven by code. Detail: `docs/dev/reviews/review-260828.md`. — fixed in a6a5e12 (PR #432, squashed to 4e7170f). The decision now lives in ONE function, `nec_solver::feedpoint_drive_voltage`, called by both the CLI's feedpoint rows and the shared input-power producer, so sabotaging that single function moves the CLI's V_RE column and the GUI's correction together -- verified, reverting it puts the GUI back to exactly 6.3372. MEASURED peak gain on a GN 2 near-ground dipole differing by its EX card alone: EX 0 gives CLI 0.2997 / GUI 0.2997 before and after; EX 4 gives CLI 0.5590 / GUI 6.3372 before / GUI 0.5590 after. 6.3372 is the number FND-053 recorded as the GUI's PRE-FIX value for the VOLTAGE case, so that earlier fix was wired at a seam one drive type slips past. The CLI moved onto the shared producer as well, verified number-identical. A current source with no port voltage is SKIPPED rather than priced with the delta-gap expression, because that fallback is the bug preserved for the next forgetful caller. THE TEST I FIRST PLANNED WAS FALSE: I had reported the CLI's EX 0 and EX 4 patterns byte-identical, but `diff` and `cmp` are not installed on this host and silently return success, so the comparison never ran -- they differ by 0.233 to 0.260 dB at every above-horizon point (see the FND-118 extension). The gate is therefore an ABSOLUTE pin against the CLI's own number, since a drive-to-drive comparison would assert something untrue and would also pass if the correction were skipped for both. New fixture corpus/dipole-ex4-gn2-near-ground-51seg.nec closes the gap that made the class ungated; its reference row was sabotage-verified as actually read. |
| FND-113 | 2026-08-28 | fixed | **[high] u32 tag-arithmetic overflow in GR/GM geometry replication panics (debug) / silently corrupts tags (release)** | Found by the 2026-08-28 whole-project audit (R08); confirmed by adversarial verification. HELD. geometry.rs:809 `seg.tag += gr.tag_increment * copy_idx` unchecked u32; reproduced exit 101 'attempt to multiply with overflow' at the cited line:col. Also kills `fnec worker --stdio` (WORKER_EXIT=101, no JSON line emitted); no catch_unwind anywhere. No [profile] section in any Cargo.toml -> release wraps silently. Corrections: PyO3 0.23 raises PanicException rather than aborting the interpreter; the release-wrap wrong-answer is inference, not demonstrated. Detail: `docs/dev/reviews/review-260828.md`. — fixed in 0719bfe (PR #432, squashed to 4e7170f): both sites use checked arithmetic and return GeometryError::TagOverflow, which the worker's exhaustive SolveError match maps to unsupported_config rather than the misleading parse_error. The GR overflow is in the MULTIPLY, one copy before the add, so it is hoisted out of the inner loop, where it was always loop-invariant, and checked there. Sabotage-verified, reverting all three checks makes both new tests fail with the original overflow panics while the ordinary-GR negative control stays green. |
| FND-112 | 2026-08-28 | open | **[medium] GUI Currents and Pattern tabs solve a plane-wave receive deck with an all-zero RHS instead of refusing, rendering all-zero currents and a meaningless pattern** | Found by the 2026-08-28 whole-project audit (R63); confirmed by adversarial verification. EXECUTED with a real cargo test -p nec-gui run (probe test appended, backed up with cp, restored with cp, NO git checkout; tree verified clean afterwards). OBSERVED VALUES on corpus/dipole-ex1-freesp-51seg.nec: CURRENTS returns Ok with n=51 and every segment current exactly 0.0 mA (max_ma=0e0, feed_ma=0e0) -- no error, no caveat. PATTERN returns Ok with 37 points all at the -999.99 dBi log floor. IMPEDANCE returns Err 'no driven feedpoint (EX voltage source) found in deck', and that refusal is raised LATE, inside feedpoint_impedance, after the solve has already run. Mechanism confirmed exactly as read: pre_solve_error is only geometry -> mixed_excitation -> frequency, none rejecting a lone plane wave; build_hallen_rhs continues past PlaneWave and returns an all-zero rhs and cos_vec; solve_hallen has no zero-RHS guard and its regularized normal equations return x=0; apply_ex returns Ok... Detail: `docs/dev/reviews/review-260828.md`. |
| FND-111 | 2026-08-28 | open | **[medium] TL card layout is fnec-specific; a standard NEC-2 TL card is misread (best case: dropped with a misleading 'z0 must be &gt; 0' warning), and the TL/NT model is a series-Z coupling, not NEC's shunt network insertion** | Found by the 2026-08-28 whole-project audit (R59); confirmed by adversarial verification. Layout claim PROVEN and the real behaviour is WORSE than the finder stated. nec2c's own DATA CARD echo is 'TL 1 11 2 11 5.00000E+01 1.50000E+00 0 0 0 0' -- four integers then Z0/length as floats -- which fnec does not implement (it reads SIX integers first). Two failure modes reproduced on a 2x21-seg dipole pair at 30 MHz: (a) the documented one -- a standard card gives 'z0=0 must be > 0; TL card ignored', exit 0, and the feedpoint is BYTE-IDENTICAL to the deck with the TL line deleted; the exponent-format variant behaves the same because parse_u32 casts floats, so '5.00000E+01' becomes NSEG=50. (b) NOT IN THE FINDING AND MORE SERIOUS: a standard card WITH nonzero shunt admittances, 'TL 1 11 2 11 50.0 1.5 0.02 0.01 0.02 0.01', is accepted with NO WARNING AT ALL and gives 10.914-j21.865 vs the no-TL baseline 11.992-j21.433 -- fnec silently modelled a 0.02 ohm, 1 cm line instead of a 50... Detail: `docs/dev/reviews/review-260828.md`. |
| FND-110 | 2026-08-28 | open | **[medium] corpus/README.md claims every deck is validated against an external NEC engine; the recorded reference engine is fnec itself** | Found by the 2026-08-28 whole-project audit (R53); confirmed by adversarial verification. README says the corpus validates 'against NEC reference engines (primary: xnec2c, fallback: 4nec2)' and 'Every NEC deck in this corpus is validated against a reference engine'; reference_engine field reads 'fnec Hallen solver (regression) / Python MoM (dipole-freesp)'. Only 8 of 48 cases carry an external_reference_candidate, and those are explicitly pending, not gating. TEMPERING FOUND BY THE VERIFIER: the doc partially self-corrects further down -- per-case 'current regression gate' labels and an open checklist item saying full parity remains open until external-reference coverage closes, plus a note that xnec2c hangs in headless CI. So it is a stale top-of-doc summary, not an unacknowledged fabrication. Detail: `docs/dev/reviews/review-260828.md`. |
| FND-109 | 2026-08-28 | fixed | **[medium] --sweep-config range mode has no point-count cap: a slipped decimal aborts the process on allocation, and NaN passes every validation check** | Found by the 2026-08-28 whole-project audit (R52); confirmed by adversarial verification. Both citations exact; every validation is an ordering comparison with no is_finite. Reproduced BOTH legs: range 1..30000 step 1e-7 under ulimit -v -> 'memory allocation of 2147483648 bytes failed', exit 134, before any solve and with no diagnostic; points_mhz=[nan, 14.2] parsed and ACCEPTED, probe reported freq_points=2, then exit 1 blaming the solver ('the solved current ... is not a finite number, so the solve did not converge'). Three guarded frequency entry points (GUI MAX_SWEEP_POINTS with is_finite rejects, solver MAX_FR_POINTS, validate.rs frequency_error), one unguarded. Not downgraded further because the NaN half yields an actively misleading diagnosis pointing at the solver instead of the input. Detail: `docs/dev/reviews/review-260828.md`. — fixed in 0460377 (PR #432, squashed to 4e7170f) (shared frequency-usability gate; sabotage-verified). |
| FND-108 | 2026-08-28 | open | **[medium] Plane-wave receive decks are solvable only by the CLI; the GUI, fnec_py and the worker refuse them with a message that does not even say the CLI can** | Found by the 2026-08-28 whole-project audit (R46); confirmed by adversarial verification. Ran the corpus plane-wave deck: CLI exit 0, SOLVER_MODE hallen-planewave, full CURRENTS table. grep confirms solve_hallen_planewave/build_planewave_hallen have call sites ONLY in nec-cli and nec_solver's own tests -- no GUI, no fnec_py, no worker. Refusal path traced: unpriceable_feedpoint_error returns None unless a CurrentSource exists, so plane-wave decks hit the generic 'no driven feedpoint (EX voltage source) found' fallback in GUI and py, and the worker returns NoFeedpoint with a test pinning that outcome. Kept medium: the wording is factually accurate and those APIs contract to return an impedance, which is undefined for a receive deck; the defect is an unactionable message plus three frontends lacking an exported capability. SPAWNED A NEW FINDING (R63) from this verifier's discovery about the GUI currents/pattern path. Detail: `docs/dev/reviews/review-260828.md`. |
| FND-107 | 2026-08-28 | open | **[medium] nec_solver's `gpu` cargo feature is a no-op: it pulls in nec_accel that nothing in the crate references** | Found by the 2026-08-28 whole-project audit (R39); confirmed by adversarial verification. nec_solver's `gpu = ["dep:nec_accel"]`; zero cfg(feature) hits in the crate's src, and the only nec_accel mention is a prose doc-comment. cargo build --features gpu succeeds and cargo tree confirms nec_accel v0.17.0 enters the graph with no source-level consumer. No workspace member enables it. Detail: `docs/dev/reviews/review-260828.md`. |
| FND-106 | 2026-08-28 | open | **[medium] FNEC_ACCEL_STUB_GPU is documented as a live testing control and manipulated by 14 test files, but no shipped code reads it** | Found by the 2026-08-28 whole-project audit (R38); confirmed by adversarial verification. cli-guide.md:749 documents FNEC_ACCEL_STUB_GPU as a live control; grep across all src trees returns ZERO hits (exit 1). ph7-chk-001-gpu-stub-retirement.md explicitly documents its removal from nec_accel. Exactly 14 test files still set/env_remove it -- genuine env manipulation, not comments, spot-checked in two. Inert plumbing for a control that no longer exists. Detail: `docs/dev/reviews/review-260828.md`. |
| FND-105 | 2026-08-28 | open | **[medium] CLI auto-exec 'startup execution probe' reads a hardwired stub, so auto mode can never select GPU; the real adapter check lives at a different seam** | Found by the 2026-08-28 whole-project audit (R37); confirmed by adversarial verification. dispatch_frequency_point is an unconditional FallbackToCpu with no cfg/feature gate, so gpu_available is compile-time-constant false and two auto branches are reachable only from hand-built test probes. Ran on this host (which has a working RADV GPU): 'gpu_available=false ... selected_exec=cpu' -- a constant presented as a probe result. Stale tracker confirmed. CORRECTION/DOWNGRADE: the finder framed this as users losing GPU speed, but warnings.rs records the GPU-resident solve as 0.04x-0.48x of CPU at every tested size, so declining it costs nothing. What remains is an honesty/dead-code defect. Also cli-guide.md:376 is stale in the OPPOSITE direction. Detail: `docs/dev/reviews/review-260828.md`. |
| FND-104 | 2026-08-28 | open | **[medium] hosts.toml scheduling overrides are parsed, documented as live controls, and read by nothing** | Found by the 2026-08-28 whole-project audit (R36); confirmed by adversarial verification. SshWorkerHandle never reads the two override fields, so detect_capability cannot apply them, contradicting probe_capability's doc comment which is simply false. assignment_weight/CapabilityCache/connect_all have only definitions, doc comments, re-exports and #[cfg(test)] references. No 'not yet wired' note anywhere. CORRECTION: the shipped path is NOT plain round-robin -- main.rs:786 calls dispatch_batch, a work-stealing pull loop that already self-balances by speed, so the practical loss is mostly the documented 'cap a shared node' case. Detail: `docs/dev/reviews/review-260828.md`. |
| FND-103 | 2026-08-28 | open | **[medium] Save writes the edited document to whatever deck_path currently says — not the file it was loaded from — and mark_saved clears the dirty flag for edits made during the async write; all deck writes are non-atomic** | Found by the 2026-08-28 whole-project audit (R34); confirmed by adversarial verification. spawn_save's own comment says 'write it back over the loaded path' but the code clones the LIVE deck_path; EditorState records no loaded-path field to bind to, and DeckPathChanged does not clear or reload the editor document. The path field is global chrome editable on the Editor tab itself, so load A / retype B / Save truncates B with A's text. Default config, ordinary click sequence. mark_saved race is real but sub-millisecond and encoded by an existing test. Non-atomic writes confirmed at three sites. CORRECTION: project_cmd.rs already reads before writing with an explicit comment, so in-place convert cannot lose data to a render failure. Detail: `docs/dev/reviews/review-260828.md`. |
| FND-102 | 2026-08-28 | fixed | **[medium] No panic containment in the worker plus retry-on-every-survivor means one poison task (deterministic crash) drains the entire pool** | Found by the 2026-08-28 whole-project audit (R32); confirmed by adversarial verification. No catch_unwind anywhere in crates/apps/bindings (rc=1), so a panic unwinds out of main with no result line; the design doc DOES define an `internal` code as 'Catch-all for unexpected panics', so the divergence is real. ssh_worker treats EOF as a dropped connection, reconnects and RESENDS THE SAME task; pool.rs then loops it over every remaining worker, ending 'all workers in pool failed'; main.rs:938 turns that into a whole-sweep FAILURE with no partial output. resource_exhausted exists only in a serde round-trip test and the enum decl -- no production path emits it. IMPORTANT NEGATIVE RESULT: the verifier drove 8 hostile decks (zero-length wire, zero/negative radius, zero segments, 1e300 coords, radius>>length, coincident wires) and ALL 8 returned clean result lines -- the solver's validation held. So no concrete poison deck is demonstrated today; only the OOM/abort class remains as... Detail: `docs/dev/reviews/review-260828.md`. — fixed in PR #439, re-scoped rather than downgraded. I had proposed downgrading it as unreachable, having capped the OOM vectors; the review showed the live vector is no longer a hostile deck but a LEGITIMATE one -- MAX_SEGMENTS permits a ~1.6 GB dense matrix, which OOM-kills a small remote host, repeats on resend, and catch_unwind cannot touch. Two parts. The amplifier: DispatchError splits on WHEN the worker died, so a task that two workers died HOLDING is blamed by name and the pool is kept, while a worker that was never reachable does not count against the task and it may keep looking for a live one. The budget is carried across both the batch and sequential loops, which previously reset it. The worker half: an unwinding panic is caught and emitted as `internal`, so it takes the hard-failure path instead of closing the channel. I HAD THIS BACKWARDS -- I thought catching it was cosmetic because a panic already surfaces as EOF, and EOF is exactly the problem, since it is what triggers reconnect-and-resend. Each half sabotage-verified separately, each failing exactly one test. Residual stated: a uniformly poison sweep still costs two workers per point. Coverage stated: not OOM, not abort, not a hung GPU device -- those are FND-101's deadline. |
| FND-101 | 2026-08-28 | fixed | **[medium] SSH/local worker dispatch has no timeout of any kind: one worker that accepts a task and never writes a newline hangs read_line forever, and dispatch_batch's thread::scope then blocks the whole sweep** | Found by the 2026-08-28 whole-project audit (R31); confirmed by adversarial verification. Zero hits for sleep/Instant/timeout across ssh_worker/pool/controller; the only Duration in the crate is capability.rs's cache TTL. ConnectTimeout=5 covers connection setup only; no ServerAliveInterval. shutdown() calls unbounded child.wait(). Design doc specifies 'up to 2 seconds for graceful exit' and 'reconnect after a 5-second back-off' -- both prose-only. AMPLIFICATION CONFIRMED: thread::scope joins every thread before returning, so one wedged read_line withholds the whole batch including other workers' completed results. CORRECTION: the 'silently dropped TCP path' sub-claim is overstated -- kernel retransmission timeout eventually errors the socket (~15 min); only the wedged-remote-process case hangs forever. Downgrade: needs opt-in --hosts plus a half-dead peer; no wrong numbers. Detail: `docs/dev/reviews/review-260828.md`. — fixed in PR #439: three deadlines, each with its own reason. Solve 15 min, matched to the kernel TCP retransmission bound so the guarantee is 'never worse than a dead socket, bounded where it was infinite', and more than five times the slowest legitimate point on this hardware. Probe 30 s, because connect_all probes hosts SERIALLY at startup before any pool exists. Shutdown 2 s then kill, the figure the design doc had promised in prose while calling an unbounded wait. ONE reader thread per WORKER, not per dispatch: my own first proposal was per dispatch and would have leaked one per wedged worker; this one exits by itself, because a timeout evicts the handle, Drop kills the child, and that closes the pipe. RESIDUAL, recorded not hidden: only the READ is bounded. A write can still block if the child stops draining stdin and the task line exceeds the pipe buffer; covering it means moving the write into the reader thread. Noted at the seam. Gated on a stub that reads its task and never answers -- not a crash and not a disconnect, the only case that hung forever. Because sabotage would HANG rather than fail, the batch runs on a spawned thread and the test waits on its own bound, so removing the deadline fails in 30 s with a message naming the cause. |
| FND-100 | 2026-08-28 | fixed | **[medium] extremes_mhz judges the untruncated sweep while frequencies_mhz truncates: one run warns 'only the first 100000 are solved' then refuses for point 400001 that would never run** | Found by the 2026-08-28 whole-project audit (R26); confirmed by adversarial verification. extremes_mhz indexes the card's FULL requested length while frequencies_mhz caps at MAX_FR_POINTS. Reproduced: FR 0 400001 0 0 30.0 -0.0001 warns 'only the first 100000 are solved' then refuses because the sweep 'reaches -10 MHz' -- a point it just said it would not solve. The 100000 solved points are all positive. Also reproduced the overflow variant. The doc comment claiming validator and expander 'cannot come to different answers' is false exactly when is_truncated(), and the existing test only exercises non-truncated cards. Detail: `docs/dev/reviews/review-260828.md`. — fixed in 0460377 (PR #432, squashed to 4e7170f) (shared frequency-usability gate; sabotage-verified). |
| FND-099 | 2026-08-28 | open | **[medium] sorted_sweep_rows excludes SweepPhase::Failed, so a failed sweep renders its chart with kept points above an empty table** | Found by the 2026-08-28 whole-project audit (R25); confirmed by adversarial verification. sorted_sweep_rows's let-else omits SweepPhase::Failed while sweep_points() includes it and SweepComplete(Err) deliberately preserves points into Failed(e, kept). So on a mid-sweep failure the chart, cursor readout and status line all show kept points while the table below shows headers and nothing. Stale comment confirmed verbatim in sweep_stream.rs:63-66. sorted_sweep_rows has exactly two references and no test. Detail: `docs/dev/reviews/review-260828.md`. |
| FND-098 | 2026-08-28 | fixed | **[medium] fnec worker --stdio answers any wire-supplied frequency_hz — negative frequency returns the conjugate impedance as status ok** | Found by the 2026-08-28 whole-project audit (R23); confirmed by adversarial verification. solve_inner's only gate is pre_solve_error(deck,segs,ground), whose signature takes no frequency at all -- frequency_error inspects only the FR card, never the wire value actually solved. Reproduced: frequency_hz=-14.2e6 -> status ok, 74.243-j13.900, exact conjugate of the +14.2e6 task, warnings []. Same value in an FR card exits 1 with a refusal. Corrections: the finding's im_ohm figure was wrong; 1e-300 IS caught, just with a downstream diagnosis. Downgrade: fnec's own controller feeds an already-validated sweep, so it needs hand-fed stdin or a skewed controller. Detail: `docs/dev/reviews/review-260828.md`. — fixed in 0460377 (PR #432, squashed to 4e7170f) (shared frequency-usability gate; sabotage-verified). |
| FND-097 | 2026-08-28 | fixed | **[medium] Negative multiplicative FR ratio solves at negative frequencies with no refusal and no warning** | Found by the 2026-08-28 whole-project audit (R22); confirmed by adversarial verification. Root cause is exactly the false invariant in extremes_mhz's doc comment: magnitude-monotone is true, sign-monotone is not. Odd point count puts a negative frequency strictly between two positive extremes. Reproduced: FR 1 3 0 0 10.0 -2.0 -> exit 0, FREQ_MHZ -20.000000, Z=251.607-j630.485, no warning. Controls confirm the guard otherwise works: linear negative refused, and the EVEN multiplicative twin is refused too (negative lands on an extreme). Downgrade: requires a hand-typed multiplicative FR with a negative ratio AND an odd count; loud (prints FREQ_MHZ -20) rather than subtle. Detail: `docs/dev/reviews/review-260828.md`. — fixed in 0460377 (PR #432, squashed to 4e7170f) (shared frequency-usability gate; sabotage-verified). |
| FND-096 | 2026-08-28 | fixed | **[medium] Worker stdin reader has no line-length limit: a single enormous line grows a String unbounded** | Found by the 2026-08-28 whole-project audit (R21); confirmed by adversarial verification. worker.rs:12-37 `for line in reader.lines()` unbounded; the 4 MiB limit exists ONLY as prose in protocol.rs:7 and distributed-execution-design.md:65, enforced at none of the three read sites (also ssh_worker.rs:147/157, controller.rs:59-61). Reproduced bounded: exit 134 at the 1 GiB ceiling. Downgrade: trust model is SSH ed25519 to operator-owned nodes, so the peer is authenticated; worst case is self-DoS of one process. Detail: `docs/dev/reviews/review-260828.md`. — fixed in PR #438: the worker read loop used `reader.lines()`, which grows a String until the line ends, while protocol.rs has documented a 4 MiB maximum since the crate's first commit. A limit that exists only in prose is not a limit. The bound is now on the READ itself, so the allocation never happens -- which also lets the test plant an over-long line without building the thing it guards against, the trap the FND-125 cap tests fell into. The refusal drains the remainder of the over-long line, or its tail would be read as the next message and answered as one; the test pins that the next real task is still answered. |
| FND-095 | 2026-08-28 | fixed | **[medium] --sweep-config range expansion is an unbounded while-loop that also bypasses MAX_FR_POINTS entirely** | Found by the 2026-08-28 whole-project audit (R20); confirmed by adversarial verification. sweep_config.rs:123-125 `while f <= end + step*1e-9` with no point cap; main.rs:243-252 REPLACES the FR expansion so MAX_FR_POINTS is never reached. Third sibling: GUI has MAX_SWEEP_POINTS=100_000 checked BEFORE expanding, solver has MAX_FR_POINTS, CLI sweep-config has neither. Reproduced bounded: exit 134, 4 GiB allocation failed. Also degenerate: for large start with tiny step, `f += step` is a float no-op and the loop never terminates. Detail: `docs/dev/reviews/review-260828.md`. — fixed -- or rather, ALREADY FIXED, and closed as a duplicate of FND-109 rather than re-fixed. Both rows describe the same defect from two finders: FND-109 says range mode has no point-count cap, FND-095 says the expansion is an unbounded while-loop bypassing MAX_FR_POINTS. FND-109's fix (PR #432, squashed to 4e7170f) computes the span BEFORE expanding and refuses. Verified rather than assumed: a range asking for 1e12 points exits 1 with 'frequency range 1..1000000000 MHz in steps of 0.001 asks for 999999999001 points; the limit is 100000'. Recorded here so the row is not left open looking like work. |
| FND-094 | 2026-08-28 | fixed | **[medium] NE/NH near-field grid allocates nx × ny × nz points with no bound on the product** | Found by the 2026-08-28 whole-project audit (R19); confirmed by adversarial verification. solve_session.rs:472-514 near_field_grid_points, nx*ny*nz from parse_u32, no aggregate cap. Reproduced bounded: 'memory allocation of 6442450944 bytes failed', exit 134. File-local fn -> CLI-only. Modest per-axis values (500 each) already exceed memory. Detail: `docs/dev/reviews/review-260828.md`. — fixed in PR #438 with FND-093, same gate and same cap. `NE 0 500 500 500` requested 3221225472 bytes and aborted. Refused at the deck level rather than at `near_field_grid_points`, because the product is visible in the card and one refusal reaches all four frontends through the gate they already share. |
| FND-093 | 2026-08-28 | fixed | **[medium] RP pattern grid allocates n_theta × n_phi points with no bound on the product** | Found by the 2026-08-28 whole-project audit (R18); confirmed by adversarial verification. farfield.rs:735-757 Vec::with_capacity(nt*np), both from raw parse_u32, no cap in the chain. Reproduced BOUNDED (ulimit -v): 'memory allocation of 40000000000 bytes failed', exit 134. CORRECTION: CLI-only -- GUI builds its grid from compile-time LOBE_N_THETA/LOBE_N_PHI, worker and bindings never mention FarFieldPoint. Downgrade: fail-fast abort with a diagnostic before output; segment count is equally uncapped, so trusting the deck author for sizing is the codebase-wide posture. Detail: `docs/dev/reviews/review-260828.md`. — fixed in PR #438: refused in the `pre_solve_error` chain before anything is allocated. `rp_card_points` sizes its vector with `Vec::with_capacity(n_theta * n_phi)`, so it asked the allocator for the whole grid in ONE call -- measured under ulimit, `RP 0 65535 65535` requested 68717379600 bytes and aborted the process. Cap MAX_GRID_POINTS = 1e6 against a largest-anywhere-in-repo grid of 2701 points. The product is computed in u64: in u32, the type the cards arrive in, 65536 x 65536 is ZERO, so a u32 check would have waved the largest possible request through as the smallest. |
| FND-092 | 2026-08-28 | open | **[medium] Total segment count is unbounded: GW segments × wire-count × GM/GR multipliers feed an uncapped N×N Z-matrix** | Found by the 2026-08-28 whole-project audit (R17); confirmed by adversarial verification. Confirmed at cited lines; no MAX_SEG/segment_limit anywhere in the tree, only frequency-axis caps. Three bounded repros all exit 134. CORRECTION: 'nothing at all stands between the deck and the allocation' is too strong -- a segment-aspect-ratio guard (L/r) does reject some decks incidentally, though it is trivially sidestepped by lengthening the wire. Downgrade: no corruption, no wrong result, no trust boundary crossed -- the deck is the operator's own input on all three surfaces. Genuine part: a small deck yields an uncatchable SIGABRT instead of a graceful refusal, and in fnec_py that aborts the host interpreter. Detail: `docs/dev/reviews/review-260828.md`. |
| FND-091 | 2026-08-28 | open | **[medium] Traceability GAP check accepts a 'VERIFIES:' citation anywhere in any source file, not 'in a test' as the register contract states** | Found by the 2026-08-28 whole-project audit (R13); confirmed by adversarial verification. collect_dir accepts ANY line of ANY .rs file containing VERIFIES: -- no check that it sits in a #[test] fn, a #[cfg(test)] module, a tests/ dir, or even a comment rather than a string literal. requirements.toml's own field doc says verification='test' means cited by a VERIFIES: IN A TEST, so the gate is weaker than its stated contract. SABOTAGE-VERIFIED: deleted the real VERIFIES: FR-003 from corpus_validation.rs and appended it to the END of nec_solver/src/lib.rs (production code, executed by nothing) -> cargo test traceability EXIT 0, 'ok'. Second implementation of the same scan in gen-traceability-matrix.py. ADDITIONS: requirements.toml names gen-traceability-matrix.SH which does not exist (only .py); and bindings/ is never walked by either scanner, so a PyO3-verified requirement is a GAP no matter how cited. Today's 9 citations are all genuinely in tests, so the defect is latent. Detail: `docs/dev/reviews/review-260828.md`. |
| FND-090 | 2026-08-28 | open | **[medium] Findings-ledger checker silently ignores any row without a space after the leading pipe** | Found by the 2026-08-28 whole-project audit (R12); confirmed by adversarial verification. check-findings-ledger.py:38 `if not line.startswith('/ FND-'): continue` runs BEFORE the tolerant ROW regex and before the malformed-row branch, so a row whose first pipe lacks exactly one space is skipped rather than reported. Reproduced: planted `/FND-999/ not-a-date / bogus-state /` -> EXIT 0, 'findings ledger OK - 62 finding(s), 0 open', 63rd row uncounted. Negative control (one space added) -> EXIT 1 with two problems, proving the bypass is the leading-pipe literal. Gate is live in check-all.sh and ci.yml. Same fragile literal duplicated at line 88, so the OPEN count under-reports too. No self-test file, unlike the release-tag checker. Detail: `docs/dev/reviews/review-260828.md`. |
| FND-089 | 2026-08-28 | open | **[medium] Doc-attachment gate is blind after the first '#[cfg(test)]' line and masks splices on duplicate item names** | Found by the 2026-08-28 whole-project audit (R11); confirmed by adversarial verification. Both fail-opens sabotage-verified with matched controls. in_test never resets: 5 files lose items, and nec_worker/{protocol,capability,solve}.rs open with the test module so 100% of their production items are skipped -- 49 items, 3.2% of the 1474 claimed 'checked'. Collision: 13 names across 7 files; base-diff check disabled for those. FND-061's '1406 items, zero detached' is therefore an inflated coverage number. Docs-hygiene gate only; no user-visible wrong answer. Detail: `docs/dev/reviews/review-260828.md`. |
| FND-088 | 2026-08-28 | open | **[medium] Benchmark regression gate reads its exit code through a pipe — 'gate=failed' is unreachable in CI** | Found by the 2026-08-28 whole-project audit (R10); confirmed by adversarial verification. benchmark-dashboard.yml:47-64 tests tee's status; no step sets `shell:` so Actions uses `bash -e {0}` without pipefail. Reproduced end-to-end: report ends 'Result: FAILED', branch taken was gate=passed; script alone exits 1. So gate output can never read 'failed' and the informational warning step is unreachable. Downgrade: the workflow explicitly declares this step informational and never build-failing; the real logic gate is the separate gate-injection-test job, unaffected. Detail: `docs/dev/reviews/review-260828.md`. |
| FND-087 | 2026-08-28 | open | **[medium] Unbounded input-controlled allocation/compute from GW segment count and GR repeat count (no cap analogous to MAX_FR_POINTS)** | Found by the 2026-08-28 whole-project audit (R09); confirmed by adversarial verification. expand_wire `for i in 0..gw.segments` and apply_gr `for copy_idx in 1..=gr.count` both unbounded; no MAX_SEG anywhere. Downstream O(N^2) sites real (validate.rs:45 double loop, matrix.rs:90 vec![_; n*n]). Reachable from worker solve.rs:371 and fnec_py lib.rs:69/359. Corrections: the finder's own 4e6 repro hangs in the intersection loop and never reaches the n*n fill; short segments trip an unrelated tiny-segment rejection first. Verifier demonstrated the alloc abort separately with long segments (exit 134, 6.4 GB). Downgrade: availability only, self-inflicted on operator-supplied decks. Detail: `docs/dev/reviews/review-260828.md`. |
| FND-086 | 2026-08-28 | open | **[medium] cli-guide is behind the shipped binary: no `fnec project convert` anywhere in the guide, and the emitted SWEEP_POINTS section is absent from the report contract** | Found by the 2026-08-28 whole-project audit (R07); confirmed by adversarial verification. All three sub-claims verified. `project convert` appears 0 times in cli-guide despite the binary printing it in its own usage and roadmap GAP-015 citing it as the entry point. SWEEP_POINTS appears 0 times in the guide though the 'Output format' section declares the report contract 'stable, versioned' and enumerates every other section; a run confirmed SWEEP_POINTS is emitted on every multi-frequency text run and report_contract.rs locks it -- so the guide is the one diverged copy. `fnec worker` absent from the guide AND from the binary's usage. CORRECTION: the guide is not wholly silent on workers (it covers --hosts and a distributed-sweep section); what is missing is the worker --stdio subcommand. Detail: `docs/dev/reviews/review-260828.md`. |
| FND-085 | 2026-08-28 | open | **[medium] python-bindings.md claims 'Hallen solver only (no ... selection from Python yet)' — fnec_py has had a solver= kwarg (including "mpie") and current-source solving since #413** | Found by the 2026-08-28 whole-project audit (R05); confirmed by adversarial verification. python-bindings.md:123 'Hallen solver only (no ... selection from Python yet)' is false: lib.rs:287/319 both carry #[pyo3(signature = (deck, solver = "hallen"))], solver_from_name accepts 'mpie', and MPIE decks route to solve_mpie_session. Documented signatures are stale (no solver parameter shown). Corrections: the parenthetical about pulse/continuity is about BASIS choice not solver kind, so only the leading clause is false; the current-source half is an omission not a falsehood. Detail: `docs/dev/reviews/review-260828.md`. |
| FND-084 | 2026-08-28 | open | **[medium] json-output-schema.md promises an empty JSON array `[]` for a no-FR deck; the binary emits zero bytes, so the doc's own Python example crashes** | Found by the 2026-08-28 whole-project audit (R02); confirmed by adversarial verification. json-output-schema.md promises [] for a no-FR deck; main.rs:254-256 early-returns SUCCESS ~270 lines before the only JSON emitter, so stdout is 0 bytes and json.loads('') raises. Control holds: FR-but-no-EX emits []. Text mode also prints nothing at all. One-line fix (emit [] before the early return). Detail: `docs/dev/reviews/review-260828.md`. |
| FND-083 | 2026-08-28 | open | **[medium] README feature list describes EX 1/2/4/5, PT, and NT as 'semantics pending / runs as EX-0 with a warning' — all six shipped over a year of releases ago** | Found by the 2026-08-28 whole-project audit (R01); confirmed by adversarial verification. README claims EX1/2/4/5,PT,NT are deferred/warn-only; all four actually solve (EX4 -> hallen-current-source Z=74.23+j13.90; NT moved Z to 6545+j1119; PT -1 suppressed CURRENTS). card-support-matrix table is right but its own intro prose is ALSO stale -> three-way divergence. README has zero occurrences of 'mpie' or 'sommerfeld'. Staleness is ~2 months / 7 releases, not 'over a year'. Docs-only, errs toward understating capability. Detail: `docs/dev/reviews/review-260828.md`. |
| FND-082 | 2026-08-28 | open | **[low] Any wire touching z = 0 is refused as 'buried' for PEC/finite ground — the canonical ground-mounted quarter-wave monopole cannot be solved** | Found by the 2026-08-28 whole-project audit (R62); confirmed by adversarial verification. buried_wire_geometry_error rejects min(z)<=1e-9 for PEC/SimpleFiniteGround as cited; a ground-mounted monopole (GW base at z=0, GN 1) hard-errors 'unsupported buried-wire geometry ... deferred' while nec2c solved the identical deck without complaint. CORRECTION: the finder's '~36+j21 ohm' figure was not reproduced (the verifier's decks were not precisely resonant) -- illustrative, not load-bearing. IMPORTANT CONTEXT THE FINDER MISSED: this is a KNOWN, deliberately scoped, already-tracked limitation (PH2-CHK-002, docs/nec4-support.md, docs/corpus-validation-strategy.md) with a dedicated regression case (dipole-gn2-buried-unsupported) locking the fail-fast behaviour in BY DESIGN. A documented non-goal that fails loud with an actionable message -- belongs in the report as a scope note, not a defect. Detail: `docs/dev/reviews/review-260828.md`. |
| FND-081 | 2026-08-28 | open | **[low] Negative-resistance caveat is suppressed for Pulse, Continuity AND Sinusoidal solver modes — including the mode the CLI's own warning recommends as accurate** | Found by the 2026-08-28 whole-project audit (R61); confirmed by adversarial verification. Reproduced with a real sinusoidal-basis solve (forced via --sin-fallback-rel-max 1.0 so SOLVER_MODE really was sinusoidal, not a hallen fallback) on a degree-3 T/Y deck: Z_RE = -37.67 ohm with ZERO stderr lines matching 'negative'. Same deck under --solver pulse: Z_RE = -665.8, also zero. So sinusoidal really is exempt from the negative-R caveat while warnings.rs:30-38 names it as the ACCURATE alternative to pulse/continuity -- a genuine inconsistency with validate.rs:249's internal 'sinusoidal is equally experimental' rationale. CORRECTION: the finder's 'no signal at all' is overstated for this repro -- the T/Y-junction and on-a-junction warnings still fire under every solver mode, so a junctioned deck does get caveats, just not the negative-R one. The literal claim holds only for a NON-junctioned deck going negative purely from basis divergence. Deliberate documented exemption on a ... Detail: `docs/dev/reviews/review-260828.md`. |
| FND-080 | 2026-08-28 | open | **[low] --pulse-rhs nec2 (the pulse solver's default) is a bare −1/λ RHS scale on an SI-units matrix: reported Z is exactly −λ·Z_raw, giving uncaveated negative resistance on a textbook dipole** | Found by the 2026-08-28 whole-project audit (R60); confirmed by adversarial verification. scale_excitation_for_pulse_rhs is a bare -v/lambda. apply_ex ALREADY delivers the correct SI EFIE RHS (its own comment: 'E = V / delta_l'), so the extra 1/lambda makes it V/m^2 against an SI Pocklington matrix with no wavelength normalisation. MEASURED: raw 16.370957+j46.798616, nec2 -345.626023-j988.019173, and -lambda*Z_raw = -345.6260168-j988.0191635 -- matches to printed precision. CORRECTION TO FRAMING: the negative-R silence is NOT an oversight but a documented deliberate exclusion (solve_session.rs:663 doc comment cites a current-source corpus with documented negative-R values) -- a decision to argue with, not a missing guard. pulse_rhs_scaling.rs pins the scale against itself, a tautology. Downgrade: non-default experimental mode with a loud banner, and its 'raw' alternative is itself badly wrong (16.4 vs 74.2 ohm), so no accurate result is lost. Detail: `docs/dev/reviews/review-260828.md`. |
| FND-079 | 2026-08-28 | open | **[low] Junction-detection tolerance 1e-6 is a bare literal repeated at 12 call sites across frontends** | Found by the 2026-08-28 whole-project audit (R55); confirmed by adversarial verification. 13 grep matches: 1 definition + 12 call sites, every one passing the bare literal 1e-6, spread across bindings/fnec_py, nec-cli (x2), nec-gui (x3), nec_solver (current_source, planewave, validate x2), nec_worker, and one test. No JUNCTION_TOL constant and no rationale comment anywhere in the tree. All 12 currently identical -- latent drift hazard in the constant that decides which solver runs. Detail: `docs/dev/reviews/review-260828.md`. |
| FND-078 | 2026-08-28 | open | **[low] MIN_GPU_RESIDENT_SEGS = 16 exists as two independent copies with no recorded provenance in either** | Found by the 2026-08-28 whole-project audit (R54); confirmed by adversarial verification. Two independent `const MIN_GPU_RESIDENT_SEGS: usize = 16;` declarations, neither a shared re-export, each with functional-purpose prose but no why-16 provenance. Contrast confirmed: GPU_SOLVE_MAX_REL_RESIDUAL carries a measured-separation comment. A THIRD hardcoded reference exists as prose at main.rs:584 ('the worker also declines the device for a deck under 16'). Both values agree today; nothing enforces that. Detail: `docs/dev/reviews/review-260828.md`. |
| FND-077 | 2026-08-28 | rejected | **[low] Two independent SWR implementations (GUI and worker) with already-divergent edge handling — the caveat/convention-must-be-shared rule, violated for a shipped number** Recorded for the record: the duplication is real, the divergence claim is not. | Refuted by adversarial verification. REFUTED on its load-bearing claim. Both citations correct and these ARE the only two swr/vswr implementations, feeding two frontends independently -- so the duplication observation is factually true. But the claim that the differing zero-denominator guards (den==0.0 vs den_sq<1e-100) are 'already-divergent' and could show finite SWR in one frontend and infinite in the other at /Gamma/~1 is FALSE. The verifier swept the boundary: exact z_re=-z0, perturbations to 1e-400, true IEEE subnormal den_sq to ~1e-320, and 2,000,000 random (z_re,z_im) pairs in [-1e5,1e5] at z0=50 -- ZERO divergences anywhere, both returning inf at the exact den=0 point. Structural, not incidental: both functions carry a later `gamma >= 1.0` check returning Infinity, which catches every case the differing first guards could separate, because num stays bounded away from zero wherever they could disagree. The two co... See `docs/dev/reviews/review-260828.md` section 6. |
| FND-076 | 2026-08-28 | fixed | **[low] --sweep-config accepts NaN and +inf frequencies that the sibling frequency seams refuse: `p &lt;= 0.0` is false for NaN, and no finiteness check exists** | Found by the 2026-08-28 whole-project audit (R48); confirmed by adversarial verification. points_mhz and start/step checks are all `<= 0.0`, false for NaN (and for +inf on points_mhz); no is_finite in the file; sweep-config frequencies bypass validate.rs's `hz.is_finite() && f > 0.0` entirely. VERIFIER ERROR CORRECTED BY ME: it claimed the finder's quoted diagnostic was fabricated because grep found no hits -- the message IS real, at crates/nec_solver/src/feedpoint.rs:66, as a line-wrapped string literal its single-line grep could not match. The finder was accurate. BUT the verifier's own run stands and is WORSE than either finder reported: it used the PLANE-WAVE deck dipole-ex1-freesp-51seg.nec, which has no feedpoint, so FeedpointError::NonFiniteCurrent never fires -- NaN reached exit 0 with a full report of 51 'NaN NaN NaN NaN' current rows. The finite-current guard protects driven decks only. Kept low (hand-written TOML, non-default flag, NaN visible in output) but the... Detail: `docs/dev/reviews/review-260828.md`. — fixed in 0460377 (PR #432, squashed to 4e7170f) (shared frequency-usability gate; sabotage-verified). |
| FND-075 | 2026-08-28 | open | **[low] GUI lib dead pub API: sweep_deck_path and MeshData::segment_count have zero callers** | Found by the 2026-08-28 whole-project audit (R42); confirmed by adversarial verification. sweep_deck_path and MeshData::segment_count each return only their own definition line under grep -rnw; the one other hit is a test named current_distribution_segment_count, a name collision not a call site. nec-gui is a lib as well as a bin, so these are genuine public surface. Neither carries a dormant/deprecated note. Detail: `docs/dev/reviews/review-260828.md`. |
| FND-074 | 2026-08-28 | open | **[low] ExcitationKind::is_voltage_source is an unused predicate that the neighboring doc identifies as a row-deleting footgun** | Found by the 2026-08-28 whole-project audit (R41); confirmed by adversarial verification. is_voltage_source has zero callers workspace-wide (only its definition and a doc-comment reference on the sibling feedpoint_role). The FND-031 danger callout lives only on the NEIGHBOURING method; is_voltage_source itself carries no warning and no #[deprecated], and no #[allow(dead_code)] suppresses it. Latent footgun, nothing calls it today. Detail: `docs/dev/reviews/review-260828.md`. |
| FND-073 | 2026-08-28 | open | **[low] WorkerPool::new_ssh (fail-fast pool constructor) has no callers anywhere, including tests** | Found by the 2026-08-28 whole-project audit (R40); confirmed by adversarial verification. grep -rnw new_ssh returns exactly two hits: pool.rs:61 (doc comment) and pool.rs:92 (definition). Zero call sites in shipped code, tests, examples or benches; the shipped CLI uses only new_ssh_skip_failures. Fail-fast error path is dead and untested. Detail: `docs/dev/reviews/review-260828.md`. |
| FND-072 | 2026-08-28 | open | **[low] Deck caveats and the solve/sweep result come from two independent reads of the deck file, and successive DeckWarnings tasks race with no ordering guard** | Found by the 2026-08-28 whole-project audit (R35); confirmed by adversarial verification. Two genuinely independent file reads with no shared snapshot: solve_deck_path does its own read_to_string while the refresh_warnings block spawns a second read_deck_text. The sweep path by contrast reads once and passes the string down. DeckWarnings applies unconditionally with no generation guard, in explicit asymmetry with SolveComplete's awaiting_solve() guard in the same file. Since SolverSelected is in the refresh set, toggling the picker spawns two concurrent futures with no ordering guarantee. UI-only staleness. Detail: `docs/dev/reviews/review-260828.md`. |
| FND-071 | 2026-08-28 | open | **[low] Blocking native file dialogs (and synchronous file writes) inside update() freeze the iced event loop** | Found by the 2026-08-28 whole-project audit (R33); confirmed by adversarial verification. rfd 0.14 sync FileDialog blocks the winit event-loop thread at main.rs:299/309/319, plus std::fs::write at 326 and a Session save per keystroke at 139. Sweep freeze real (channel(64) backpressure, no message loss). CORRECTIONS: the extra citation 338-353 is WRONG -- that block is already Task::perform, i.e. correctly off-thread; and there is no documented responsiveness convention, only a de facto pattern. Downgrade: user-initiated modal dialogs during which the app is unusable anyway; no wrong result, no data loss. Detail: `docs/dev/reviews/review-260828.md`. |
| FND-070 | 2026-08-28 | open | **[low] A deck with no FR card: CLI exits 0 printing nothing at all, while the GUI and fnec_py refuse it** | Found by the 2026-08-28 whole-project audit (R28); confirmed by adversarial verification. main.rs:254 `if freqs_hz.is_empty() { return ExitCode::SUCCESS; }` -- reproduced: no-FR deck gives exit 0 with stdout+stderr = 0 bytes (wc -c). GUI solve.rs:410 and fnec_py lib.rs:296 independently construct 'deck has no FR card' as an ERROR. Three-frontend divergence verified as stated. Detail: `docs/dev/reviews/review-260828.md`. |
| FND-069 | 2026-08-28 | open | **[low] CLI FR sweeps still emit one negative-resistance warning per point — the swept aggregate producer is wired to GUI and fnec_py only** | Found by the 2026-08-28 whole-project audit (R27); confirmed by adversarial verification. swept_negative_resistance_caveat is called only from nec-gui solve.rs:845 and fnec_py lib.rs:361, never from nec-cli, whose per-point warn runs inside the per-frequency solve. MEASURED: the BENT_NEGATIVE_R inverted-V over a 50-point sweep produced exactly 50 distinct 'has negative resistance' lines on stderr, each with a different Re Z. Aggregate-caveat seam with one diverged frontend; stderr noise, not wrong physics. Detail: `docs/dev/reviews/review-260828.md`. |
| FND-068 | 2026-08-28 | open | **[low] Version-bump docs gate detects the bump with the same banned sed/head grep, and is PR-only + not a required status check** | Found by the 2026-08-28 whole-project audit (R16); confirmed by adversarial verification. check-version-bump-docs.sh:22 uses the same banned sed/head first-match extraction its sibling documents as 'exactly how a check earns false confidence'; dormant today (only one version line). CI step is `if: github.event_name == 'pull_request'`. Verifier checked the live repo config: branch protection required_status_checks.contexts = [] and the only ruleset targets TAG refs, so 'docs contract' is not required anywhere and a PR can merge with it red or skipped. release-tag.yml greps changelog/releasenotes at mint time but NEVER SBOM.spdx.json, so the SBOM requirement is enforced in exactly one bypassable place. Detail: `docs/dev/reviews/review-260828.md`. |
| FND-067 | 2026-08-28 | open | **[low] Corpus provenance derives the historical version with the exact naive first-line grep its sibling checker documents as a trap — and stamps + checks with the same code, so an error would self-validate** | Found by the 2026-08-28 whole-project audit (R15); confirmed by adversarial verification. derive-corpus-provenance.py:46 takes the first line starting with 'version' and splits on quotes, while its sibling check-release-tags.py:95 uses tomllib and its docstring explicitly says 'Not a grep... exactly how a check earns false confidence'. --check recomputes with the SAME naive derivation it used to stamp, so a wrong derivation would write wrong provenance and certify it fresh. Verifier replayed all 76 commits touching reference-results.json comparing naive grep vs tomllib: 0 divergences today. Latent shape, not an active bug. Detail: `docs/dev/reviews/review-260828.md`. |
| FND-066 | 2026-08-28 | open | **[low] check-all.sh calls itself 'the full local gate' but omits four CI docs-contract checkers plus audit/deny/bindings-pytest/coverage** | Found by the 2026-08-28 whole-project audit (R14); confirmed by adversarial verification. check-all.sh line 5 says 'The full local gate, across BOTH cargo trees (FND-024)' but omits validate-docs-frontmatter, gen-traceability-matrix --check, derive-corpus-provenance --check, check-version-bump-docs, plus cargo audit, cargo deny, pytest bindings, and llvm-cov --fail-under-lines 75 -- all present in CI. Fail-closed under-claiming: CI still catches all of these, so no gate silently passes. Detail: `docs/dev/reviews/review-260828.md`. |
| FND-065 | 2026-08-28 | open | **[low] gpu_kernels rename stragglers in living project docs: implementation-map and test-catalog point at a file/module that no longer exists** | Found by the 2026-08-28 whole-project audit (R06); confirmed by adversarial verification. gpu_kernels.rs does not exist (renamed kernel_reference.rs in #430); implementation-map.md:52 and test-catalog.md:70 and shaders/rp_farfield.wgsl:11 still name it. Verifier found a straggler the finder MISSED: traceability-matrix.md:153 also lists gpu_kernels.rs for PH7-CHK-001. Also test-catalog's lib count is off by one (26 actual vs 25 claimed). Downgrade: documentation-only naming drift, nothing dispatches on these strings. Detail: `docs/dev/reviews/review-260828.md`. |
| FND-064 | 2026-08-28 | open | **[low] cli-guide, nec_accel rustdoc, and a user-visible diagnostic all cite PH7-CHK-004 as the open tracker for hybrid per-frequency GPU dispatch — PH7-CHK-004 is Done and delivered something else; the seam remains unwired with no live tracking item** | Found by the 2026-08-28 whole-project audit (R04); confirmed by adversarial verification. PH7-CHK-004 is cited as the open tracker by nec_accel/src/lib.rs:50/84/93, cli-guide.md:41/746; roadmap.md:310 marks it Done 2026-06-27 and its Done-signal text is entirely about the SSH worker pool -- different work. Deferral chain real: ph7-chk-001 doc explicitly hands the seam to PH7-CHK-004, which delivered something else and closed. No live tracker exists for the seam. Correction: the user-visible string is main.rs:436, not the internal comment the finder cited. Downgrade: the behavioural half of every message is TRUE; only the tracker ID misleads. Detail: `docs/dev/reviews/review-260828.md`. |
| FND-063 | 2026-08-28 | open | **[low] Roadmap phase headers: Phase 8 and 9 titled '(planned)' though both completed and shipped; Phase 6 still 'Estimated completion: Q4 2027' though done in June 2026; Phase 7 'Key deliverables' boxes unchecked in a '(complete)' phase** | Found by the 2026-08-28 whole-project audit (R03); confirmed by adversarial verification. All four sub-claims hold: line 316 '## Phase 8 (planned)' vs line 345 'Actual completion: 2026-07-04'; line 357 'Phase 9 (planned)' vs 'Phase 9 solver frontier is now closed'; Phase 6 all seven rows Done yet 'Estimated completion: Q4 2027'; three Phase-7 bullets unchecked whose PH7-CHK rows are Done. Finder was careful: it correctly excluded bullets 296/297 which are legitimately unchecked. Downgrade: in every case the correct status is stated in plain text a few lines below the misleading header. Detail: `docs/dev/reviews/review-260828.md`. |
| FND-062 | 2026-08-28 | fixed | **The release-tag checker misreported its own coverage.** `in_flight` was set whenever the newest changelog section matched the workspace version, *regardless of whether that version was tagged* — so from the moment a release merged until the next bump it called a tagged release "in flight", and `checked` subtracted it from the count although its tag **had** been checked. Observed live during the v0.16.0 release: `9 tag(s), 7 released version(s) checked ... 0.16.0 in flight`, with v0.16.0 tagged and verified moments earlier. It is precisely the arithmetic the comment on `checked` warns against — "count what was actually examined rather than subtracting one set size from another" — committed by the line that comment sits under. Cosmetic in effect, but a checker that overstates or understates what it examined is the one kind that must not. | #422 — in-flight now requires the tag to be *absent*. The self-test grew a helper that asserts the checker's **output**, not just its exit code: every prior case read the exit code, which is exactly why a wrong summary survived to be found by hand. Sabotage-verified — restoring the old condition reproduces the wrong line verbatim. Now 24 cases; the release run reports `8 released version(s) checked` with no in-flight note |
| FND-061 | 2026-08-28 | fixed | **A doc comment can come adrift from its item in two ways that no gate could see.** A blank line between them detaches it silently; new code inserted between them hands the previous item's rationale to the *new* item's rustdoc and leaves the original undocumented. Both compile, pass clippy, and pass every test. It happened **four times in one working session** — `validate.rs`, `SolveResult`, `fnec_py`'s `solve_at_freq`, and `distributed_negative_resistance_warnings` — three of them caught only because a reviewer was reading the file, and the fourth after a memory recording the exact rule already existed. A rule forgotten four times is not working as a rule. | #420 — `scripts/check-doc-attachment.py`, in the local gate and the CI docs job. The splice shape is invisible statically (the doc is still contiguous with *a* function, just the wrong one), so the check takes a base revision and reports an item that **had** a doc and no longer does — a diff property, not a static one. Sabotage-verified in both shapes with exit codes captured directly rather than through a pipe. Currently 1406 items, zero detached |
| FND-060 | 2026-08-28 | fixed | `process_task`'s **pre-decode** failures still label a transport fault as a deck fault. A corrupt task line, undecodable base64, or a non-UTF-8 deck all return `ErrorCode::ParseError` (`crates/nec_worker/src/worker.rs`), and the protocol doc defines that code as "deck failed to parse on the worker" — so a truncated SSH payload reads to the user as a typo in their antenna file. Same misdirection class as FND-049, one layer further out: that one was inside the solve, this one is before it. Left out of #418 deliberately — #418 made the *solver* mapping exhaustive, and the pre-decode paths are a different question (what code should a transport fault carry, given `ErrorCode` has no variant for one and cannot cheaply gain one). Recorded in the doc's `parse_error` row as a known imprecision rather than left to be rediscovered. | found by fable's diff review of #418. **#427** — the *message* carries the distinction, since the code cannot: `ErrorCode` is externally tagged with no `#[serde(other)]`, so a new variant fails an older controller's whole result line and the pool then evicts it as dead. The three pre-decode failures now open with `transport:` and say **no deck was read**, so a truncated payload no longer reads as a typo in the user's antenna file. A genuine deck fault is asserted *not* to claim it is a transport one, or the distinction would be decoration |
| FND-059 | 2026-08-28 | fixed | `TaskResult::Error` carries no `warnings`, so a deck that is **both** flawed and refused loses the flaw. A deck with an unrecognised card *and* a bad `EX` reference comes back as `unsupported_config` with the ignored card never mentioned: `solve_deck_at_frequency_with_exec` computes the parse warnings (FND-041, #418) and every `Err` path drops them. By the same reasoning FND-026 used for the `Ok` variant, adding `#[serde(default)] warnings` to `Error` is additive both ways and safe for a mixed-version pool. | found by fable's diff review of #418. **#419** — `TaskResult::Error` carries `#[serde(default)] warnings`, populated on the solve's failure path and read by the controller. The solve's caveats accumulate into an out-parameter (`solve_inner`) rather than being threaded through eight `?` exits that would each say nothing new; `solve_deck_reporting_warnings` returns them whether or not the solve succeeded, and the two existing public entry points keep their signatures. The controller destructured the error with `..`, which is precisely how the `Ok` arm's warnings went unread for a release (FND-026) — so the deciding was factored into `worker_warning_lines`, a name a test can reach (FND-034's lesson). Five sabotages, five bite, including the pre-decode paths asserting they report *no* caveats: a task refused before anything was parsed must not invent them **The first version of the fix only closed half the class**, and review caught it by walking every error exit and asking which caveats existed yet at each one: `build_deck_stamps` ran *after* the RHS build, so a deck that was both flawed (a skipped `LD`) and refused (an `EX` naming a missing segment) still reported the refusal with the flaw missing — FND-059's own sentence one exit over, reproduced live on the branch. The stamps are computed before the RHS now; they need only the deck, segments and frequency, so the ordering was never forced. Applying them stays where the matrix exists. Also added the `Error` variant's own back-compat tests: the `Ok` variant had them since FND-026 and the variant this field was *added* to had none, and the roundtrip set `warnings` without asserting them coming back — a `skip_serializing` would have left every test green. |
| FND-058 | 2026-08-27 | fixed | A **zero-amplitude source** reports `Z = 0.000000 + j0.000000` with no caveat. `EX 0 1 11 0 0.0 0.0` on an ordinary dipole gives exit 0 and a feedpoint row of zeros — the same `V/I` zero-current fallback that FND-056 and FND-050 reach by other routes. Not folded into #417: that fallback is FND-050's subject and its fixture depends on the current behaviour, so touching the division would decide a finding the maintainer has reserved. A vanishingly small but finite frequency (`FR ... 1e-300`) reaches the same place and is likewise left. | measured 2026-08-27 while fixing FND-056. **#425** — `nec_solver::feedpoint::feedpoint_impedance` is the one division now, adopted at all **six** sites. The finding named one family; review found two — four sites divide the source voltage by the solved current, and two divide the solved port voltage by the impressed current, so a zero-amplitude `EX 4` reached the same defect by a route a solved-current check never sees. It also separates **non-finite from zero**: `current.norm() > 1e-60` is false for `NaN` too, so a diverged solve took the zero branch and got told its current was zero. `MIN_FEEDPOINT_CURRENT` replaces six literals and a seventh convention. This also closes the `1e-300` residue #417 could not reach, since that frequency is finite and positive and only underflows once the matrix is built |
| FND-057 | 2026-08-27 | fixed | **Two diverged `FR` expanders.** `frequencies_from_fr` (`apps/nec-cli/src/solve_session.rs`) reads only the **first** `FR` card and treats an unrecognised `step_type` as "the start frequency alone"; `frequencies_from_deck` (`bindings/fnec_py/src/lib.rs`) reads **every** `FR` card and treats an unrecognised `step_type` as linear. So a two-`FR` deck already sweeps differently in the CLI than in `fnec_py` — the house "N copies, one diverged" class, live. #417 deliberately did **not** pick a winner: choosing between them is inventing multi-`FR` semantics, and the validator instead covers the union (linear expansion for everything non-multiplicative, which contains the CLI's single start frequency as its first element), so a deck it passes is safe on either reading. Unifying them is a separate change with its own semantics decision. | found by fable's design review of FND-056. **#424** — settled by measuring `nec2c` rather than arguing semantics, and **neither implementation was right**: `FR 14.2` then `FR 7.1` gives 13.792 − j914.16 with header `FREQUENCY : 7.1000E+00 MHz`, and reversing the cards gives the other answer — so the **last** card governs, not the first and not all of them. `FR 2 3 0 0 14.2 0.1` runs 14.2/14.3/14.4, so an unrecognised `step_type` is **linear** — `fnec_py` was right there and the CLI wrong. **The finding undercounted the copies:** review found the GUI reads the first `FR` card in three more places and `validate` expanded a fourth way, so five readings, not two. All now derive from `FrSweep::frequency_mhz` in `nec_solver::frequency` — one function, so a validator and an expander cannot disagree about the same card. Superseded cards are **warned about, not silently dropped**: such a deck is usually `FR/XQ/FR/RP`, which NEC-2 runs both halves of and fnec cannot, so silence would discard a run the author asked for. The expander also inherited the 3.2 GB allocation the validator was cured of in #417 — bounded now. Four sabotages, four bite; changed-set of existing fixtures is empty, since no deck in the repo has two `FR` cards |
| FND-056 | 2026-08-27 | fixed | **A degenerate `FR` frequency is accepted and answered with confident nonsense, on every frontend.** Nothing in `nec_parser` or `nec_solver::validate` checks `frequency_mhz` at all. Measured on `main` (e5cc774), 21-segment dipole, `EX 0 1 11 0 1.0 0.0`: `FR ... 0.0` gives `I = 0.000000 + j0.000000`, so `Z = V/I` divides by zero and prints **1.000000 + j0.000000** — the `EX` source voltage handed back as an impedance, the FND-050 signature — at exit 0 with zero warnings; `1e-300` does the same. Worse, `FR ... -14.2` returns **67.161824 + j32.275596**, the exact complex **conjugate** of the +14.2 control (67.161824 − j32.275596): a typo'd minus sign silently flips the reactance between capacitive and inductive and reports it as fact. Found while probing for FND-030's `NaN`, which I could not reach from the CLI front door — coincident duplicate wires (identical matrix rows) give a finite 267.24 − j80.63, and an absurd radius is caught by the existing source-risk gate. | measured 2026-08-27 while scoping FND-030. **#417** — `validate::frequency_error`, folded into the shared `pre_solve_error` aggregate so all four frontends refuse together (the FND-036 precedent). Checked over the **generated** frequency list, not the card's first field, because a descending sweep passes any start-value test and still walks into negative frequency — sabotage-verified: narrowing the check to `fr.frequency_mhz` alone fails `every_degenerate_frequency_class_is_refused`. The GUI's sweep range needed its own guard: it comes from the UI and never reaches an `FR` card, so a sweep from −5 MHz was invisible to the shared check, and its docstring had claimed "positive float" all along while nothing checked it. Non-finite range fields are rejected before any ordering test, since every comparison against `NaN` is false and `step <= 0.0` waved one straight through. Negative control: every corpus deck is `FR 0 1 0 0 f 0.0`, so a 0.0 step with one step stays legal **Two blockers in the first version, both found by the diff review and both fixed before merge:** `inf > 0.0` is true, so a *finite* field near the top of the range — `FR 0 1 0 0 1e303 0` — passed the check and then overflowed to `FREQ_MHZ inf` once multiplied into hertz, reproducing **both** defects from a deck the check accepted; finiteness is now tested in hertz. And the expansion collected `steps.max(1)` floats where `steps` is a `u32`, so `FR 0 400000000 ...` asked a *validator* to allocate 3.2 GB — inside `pre_solve_error`, which the GUI calls on every Apply+Solve and the worker on every task. It now takes each card's extremes, which decide the rest because both expansions are monotone. Integer fields needed the parser fix too: they go through `f64` (4nec2 emits "1.0"), so `NaN as u32` = 0 made `FR 0 NaN 0 0 14.2 0` parse and solve as a one-step sweep. |
| FND-055 | 2026-08-27 | fixed | `fnec_py` is now the only frontend without a solver choice. The CLI has `--solver mpie` and the GUI has a picker (#415); the bindings expose neither, so a Python caller with a T/Y junction or a closed loop is told to reach for the CLI. Nothing technical blocks it — `nec_solver::solve_mpie_session` is the same call the other two make. Recorded rather than done so the remaining frontend difference is a decision, the FND-051 precedent. | recorded while closing FND-007. **#428** — `solve_deck_str(deck, solver="mpie")` and the same on the sweep, defaulted to `"hallen"` so every existing caller is untouched. Routes through the shared `solve_mpie_session`, whose refusals travel inside it, and skips the Hallén assembly the MPIE never reads. The remedy in the shared diagnostics changed with it: a Python caller is told to `pass solver="mpie"` rather than pointed at the CLI, which is a different program. Pinned against the CLI's Y-junction value (63.673674 − j322.199211) so the frontends cannot drift |
| FND-054 | 2026-08-27 | fixed | Downgrading the MPIE refusal in `validate::diagnose` from `Error` to `Warning` fails **no test** — the sabotage was run and passed. The refusal is not unguarded: `solve_mpie_session` carries its own (#414), so a loaded deck is still refused rather than answered. What is uncovered is the *earlier* rejection — the one that stops a solve before it starts rather than at the solve. The safety net hides the missing check, which is the arrangement that makes a gate rot unnoticed. The sweep's own pre-check **is** covered (`an_mpie_sweep_refuses_an_unstampable_deck_up_front`, sabotage-verified); this is the `diagnose` path only. | found by sabotage-verifying #415; the passing sabotage is the evidence. **#415** — closed in the PR that found it, rather than left as a row. `an_mpie_deck_with_a_load_is_an_error_from_diagnose_not_a_warning` pins the *severity*: an `LD` deck on the MPIE must be an `Error` from `diagnose`, with the Hallén control asserting the same deck is fine there. Re-run of the original sabotage now fails, where before it passed silently. The distinction that makes the severity matter: an Error is the pre-solve refusal, while a Warning would let the strip render it and the solve fail later |
| FND-053 | 2026-08-27 | fixed | The GUI never converts directivity to gain over lossy ground. The CLI applies the PH9-CHK-003 radiation-efficiency correction in `solve_session.rs` (`radiation_efficiency` / `integrate_radiated_power`); `apps/nec-gui`'s `pattern_grid_str` calls neither, and no ledger row named this before. So a `GN 2` deck's pattern reads as gain in the CLI and as directivity in the GUI — the same deck, two different dBi numbers, with nothing saying which is which. Pre-existing and affecting **both** solvers, so not caused by the MPIE work; it becomes more visible with a solver picker, because near-ground decks over finite ground are exactly what the MPIE exists for. | found by fable's design review of FND-007. **#428** — `nec_solver::gain_correction_db` and `feedpoint_input_power` are shared, and the CLI's inline version now calls them. **Measured on `corpus/dipole-gn2-near-ground-51seg.nec`: CLI peak 0.2997 dBi, GUI 6.3355 before, 0.2997 after — the GUI was overstating gain by 6.04 dB**, which is exactly the ground loss it was not accounting for. Both GUI pattern views (full-sphere grid and elevation slice) apply it, since correcting one and not the other would put two numbers for one deck on two tabs. Honest limit on the free-space control: it fails a wrong-sign or wrong-magnitude correction, but **not** the ground-type guard — removing that guard leaves it green, because free-space efficiency is ~1. Verified by sabotage rather than assumed |
| FND-052 | 2026-08-27 | fixed | The MPIE's mixed-radius caveat was CLI-private. Its reduced thin-wire kernel solves the **whole** geometry with the first segment's radius, and `warn_mpie_mixed_radius` was an `eprintln!` in `apps/nec-cli/src/warnings.rs` rather than a `validate` producer — so the moment any other frontend gained an MPIE path, a mixed-radius deck would be solved approximately with nothing said. Exactly FND-020's shape, caught before it shipped rather than after. | #414 — `validate::mpie_mixed_radius_caveat` is now the shared producer and the CLI's warning is four lines that decide the solver mode and print it. Moved in the *same* PR as the solve, not a follow-up: a shared solve whose caveat stayed behind is the FND-048 mistake with a different subject. Found by fable's design review of FND-007, which refused the design for omitting it |
| FND-051 | 2026-08-26 | fixed | The worker still refuses a current-source deck while the CLI, the GUI and `fnec_py` now solve one (#412). That is a **scope** choice, not a technical one, and the earlier review said so: `FeedpointResult` already carries `impedance_re/im` and `current`, so `Z = V_port/i0` fits the existing wire format and the port voltage never needs to cross it. What the change needs is the GPU-resident gate excluding current-source decks, and the refusal at the extraction site removed. Recorded so the remaining frontend difference is deliberate and visible rather than an oversight nobody named. | recorded while closing FND-045. **#429** — the maintainer asked for the remaining findings, so the scope choice is reversed. It went exactly as the earlier review predicted: `FeedpointResult` already carried impedance and current, so `Z = V_port/i0` fits the wire format unchanged and the port voltage never crosses it. The worker now returns **74.227929 + j13.896926** for `corpus/dipole-ex4-freesp-51seg.nec`, identical to the CLI, GUI and bindings — all four frontends agree. The GPU-resident gate excludes the class, because that path solves a delta-gap right-hand side from raw segment inputs and cannot represent a forced current at all; sabotage-verified, since letting it through would answer with the wrong physics rather than fail. The refusal test became a parity test against the CLI's corpus value |
| FND-050 | 2026-08-26 | fixed | A plane wave alongside a driven source is the same wrong-answer shape as FND-036, one mix over. `corpus/dipole-planewave-then-source-51seg.nec` reports **Z = 1.000 + j0.000** for its `EX 0` feedpoint where a driven dipole alone gives 74.243 + j13.900 — the deck routes to the receive path, the driven current comes out ~0, and `V/I` falls back to printing the source voltage. Exit 0, no warning. Deliberately out of FND-036's scope: it is receive-versus-transmit rather than two drives, it changes how receive decks are handled, and that fixture is depended on by GUI and `fnec_py` tests, so refusing it is its own decision. | found while scoping the FND-036 refusal. **#425** — refused, after the oracle removed the doubt. nec2c on that deck (with an `XQ`) reports **79.348 + j46.223**, bit-identical to the same deck with the plane wave deleted: NEC-2 runs the last `EX` type and discards the rest rather than superposing transmit and receive, so a refusal loses no answer the reference offers. The earlier scope note feared breaking a fixture two frontends depend on; what it could not know is that those frontends were **already** giving three different answers — the CLI routed to the receive path and printed 1.000 + j0.000, while the GUI and bindings skipped the plane wave and answered ~74.24 + j13.9. The two dependent tests now assert the refusal names tag 1 segment 26, which is the same FND-031 fact they always pinned. A plane-wave-**only** deck is untouched, with its own control |
| FND-049 | 2026-08-26 | fixed | The FND-021 mislabel has a live sibling one layer up: `process_task`'s catch-all stamps `ErrorCode::ParseError` on `SolveError::GeometryError` **and** `NoFeedpoint`. Reachable and wrong — a plane-wave (`EX 1`) receive deck returns `NoFeedpoint`, so through `--hosts` it crosses the wire as `parse_error — "no driven feedpoint (EX voltage source) found in deck"` for a deck that parsed cleanly *and that the local CLI solves*, since plane-wave receive works locally. FND-021 fixed the `build_hallen_rhs` arm as its row described; this is the same class at the outer mapping. | found by fable's diff review of #410, which declined to let "fixed" read as "the class is closed". **#418** — the mapping is exhaustive now, so a new `SolveError` forces a decision at compile time instead of silently inheriting the wrong code, which is the mistake the catch-all made once already. `GeometryError`, `UnsupportedConfig` and `NoFeedpoint` all map to `UnsupportedConfig`: each is a statement about what this worker supports, not about the deck's syntax. Mapped onto existing codes rather than adding an `ErrorCode` variant, following the reasoning already recorded at the FND-021 site — the enum is serialised, so a new variant breaks an older controller's deserialisation outright, unlike the additive `warnings` field. Tested at the wire boundary through `process_task`, not at the solver beneath it, because that is where the mislabel was visible; sabotage-verified, with a genuine syntax error asserted to still earn `ParseError` so the code was narrowed rather than abandoned |
| FND-048 | 2026-08-25 | fixed | **A current source on a collinear-split wire silently delivers half the current it was given.** `solve_current_source_hallen` passes the raw per-`GW` `wire_endpoints` into `solve_hallen_current_source`, which pins `I = 0` at each card's first and last segment — while the *voltage* path merges collinear joins first (`merge_collinear_wire_endpoints`). A collinear chain is a trivial conductor path, so it skips the paths solver and lands on that branch, where the solver is asked for `I[src] = 1` and `I[src] = 0` at once and least-squares splits the difference. **Measured:** a 51-segment `EX 4` dipole gives 74.228 + j13.897 Ω with `I = 1.0`; the identical geometry written as two collinear `GW` cards gives **36.953 + j7.013 Ω with `I = 0.5`** — half the impressed current — at exit 0 with no warning. The `EX 0` control on the same split geometry gives 74.307 + j13.893, so the geometry is fine and only the current-source path is wrong. Neither corpus `EX 4` fixture is split, so nothing catches it. | #408 — the per-wire current-source solve now gets `merge_collinear_wire_endpoints`, the list the voltage path has used since PH9-CHK-002. Only the endpoints changed; the routing above decides which *solver* runs and moving a deck between solvers is a different change. `a_collinear_split_current_source_delivers_the_current_it_was_given` asserts the delivered current before the impedance — an impedance can be wrong for many reasons, but a current source that does not deliver its stated current has violated the boundary condition the user wrote. Sabotage-verified: restoring the raw list fails with "asked for 1 A; the solve delivered 0.5 A". Fixture `corpus/dipole-ex4-collinear-split-51seg.nec`. Found by fable's design review of FND-045, while checking whether the CLI glue was safe to share with the GUI and the bindings |
| FND-047 | 2026-08-25 | fixed | The repository has **no tag protection ruleset**, so any workflow job holding `contents: write` can create, move or delete every `v*` tag — including `benchmark-dashboard.yml`'s job, which runs `cargo build` (arbitrary build scripts and proc macros from the dependency tree) under that token and pins its toolchain to `@stable` rather than a SHA, against the posture `ci.yml` follows. Tag corruption is silent: no PR, no diff on `main`, and by this project's own doctrine not correctable by re-pointing. A `v*` ruleset would narrow "any write-token job" back to "the mint workflow". Needs a repository-settings change, so it is the maintainer's to make. | fixed by the maintainer 2026-08-25: ruleset 21436197 "Release tags are immutable", target `tag`, `refs/tags/v*`, rules `deletion` + `non_fast_forward`, no bypass actors, enforcement `active` (`evaluate` is not offered on the free plan). Creation is deliberately *not* restricted, so the mint workflow and hand-tagging still work while no token can silently delete or move a published tag. **This makes the immutability doctrine binding:** the v0.15.0 delete-and-retag done during that release would now be refused, and the correct response would be a v0.15.1. Found by fable's design review of #406 |
| FND-046 | 2026-08-25 | fixed | **A checker detects a missing tag; only automation prevents one.** A workflow that mints an annotated tag when a version bump merges would close FND-043's class outright, and would close the mispointed-tag half of FND-044 by construction — the tag is created at the commit whose tree declares the version, so it cannot point elsewhere. Deliberately **not** folded into #405: this project's convention is that the user cuts releases explicitly, so whether a merged version-bump PR counts as that act is a policy decision, not a hygiene fix. It also needs `contents: write`, which the least-privilege posture in `ci.yml` scopes tightly, and tags created with `GITHUB_TOKEN` do not trigger tag-triggered workflows — moot today, a landmine if a release workflow is ever added. | #406 — built as a **`workflow_dispatch` button**, not a push trigger, on evidence from this repo: at `2f51d63` (the v0.15.0 release-PR merge, which push-minting would have tagged) `pyproject.toml` says 0.4.0 against a 0.6.0 crate — the live FND-044 defect. The real tag sits two commits later at `1354fda`. Auto-minting would have created the tag the maintainer then had to delete, and a never-move rule would have made the automation defend it. The human still decides *that* a release is ready; the machine verifies the commit before tagging and refuses if it does not deserve it — demonstrated: the pre-mint check exits 1 at `2f51d63` and 0 at `1354fda`. The dropped repair mode's blind spot is closed instead by an age-aware in-flight exemption plus a weekly schedule. Hardened in #409 before its first run — the diff review that #406 skipped found that `git tag -F` strips every `#`-leading line by default, so the Keep-a-Changelog headings would have vanished from every minted tag message (four in the 0.15.0 section, zero surviving); also `checks: read`, `--paginate`, and `env:` instead of `${{ }}` in the shell |
| FND-045 | 2026-08-25 | fixed | The GUI and `fnec_py` decline a current-source deck saying the port voltage "is not computed by this path" — true, but the machinery is not missing, only unwired: `nec_solver` exports `solve_hallen_current_source` and `solve_hallen_current_source_paths`, and the CLI's part is app-level glue. So the refusal documents a gap either frontend could close with a library call rather than a limitation of the solver. | #412 — the maintainer asked for the capability (2026-08-26). The CLI's glue moved to `nec_solver::current_source` and all three GUI solve paths plus `fnec_py` route through it; an `EX 4` deck now gives 74.227929 + j13.896926 in the GUI, identical to the CLI. It was **not** safe to share until FND-048 (a collinear split delivered half the requested current) and FND-036 (a mixed deck was answered rather than refused) were fixed first — promoting it earlier would have turned one wrong frontend into four, which is what the design review declined to allow. The branch is at the *solve* step, not the pricing step: a current-driven deck's excitation vector is all zeros, so `V/I` has nothing to divide. One sabotage of the shared solve fails the CLI corpus gate and the GUI at once. Worker deliberately still refused — recorded as FND-051. Found by fable's diff review of #404 |
| FND-044 | 2026-08-25 | fixed | **v0.14.0 shipped a wheel labelled 0.4.0 while its release notes announced fnec_py 0.5.0.** `bindings/fnec_py` declares its version twice — in `Cargo.toml` and in `pyproject.toml` — and maturin stamps the *pyproject* one onto the built wheel, which is what `pip install` gives a user and what `pip show` reports. The v0.14.0 bump touched only `Cargo.toml`, so the wheel carried 0.5.0's breaking behaviour under 0.4.0's name. The release checklist's own consistency command greps `Cargo.toml` files and therefore could not see it, which is why it survived a release. | #403 — `pyproject.toml` bumped to 0.6.0 alongside the crate, and `scripts/check-binding-version.py` added to the CI docs job, sabotage-verified. Found while building the v0.15.0 wheel and noticing it was named `fnec_py-0.4.0` |
| FND-043 | 2026-08-25 | fixed | Five released versions were never tagged — v0.4.0, v0.5.0, v0.6.0, v0.8.0 and v0.9.0 all have changelog sections but no git tag, so there is no ref to compare against or check out. Found while adding the Keep-a-Changelog compare links the release checklist asks for: the links exist now only for the contiguous tagged range v0.10.0 onward. Tagging them retroactively means guessing which commit each release was cut from, which is worse than the gap; the fix is a CI check that a version bump lands with a tag. | #405 — `scripts/check-release-tags.py`: every changelog version has a tag, and every tag names the version its tree declares. The five are grandfathered as a frozen set rather than a `BEFORE` cutoff, because the gaps *interleave* with present tags (0.3.0 tagged, 0.4-0.6 not, 0.7.0 tagged, 0.8-0.9 not) and a cutoff would stop guarding v0.3.0 and v0.7.0 against deletion — a live failure mode here, since v0.15.0 was deleted and re-pushed during its own release. Runs in the docs job and on `push: tags: 'v*'`, the one moment a bad tag is catchable before anyone acts on it. Has the repo's first committed checker self-test — including fixture repositories that exercise `main()`, after review demonstrated three ways to hollow it out with every pure-function case still green. **Known limit, recorded:** a *deleted* tag is invisible for the newest release, because "newest section, no tag" cannot be told from "release in flight" from inside the repo — and the newest is the only tag anyone has actually deleted (v0.15.0, during its own release). That needs FND-046, not a better checker |
| FND-042 | 2026-08-24 | fixed | The GUI evaluates `low_finite_ground_warning` at the **`FR` card's** frequency (`deck_warnings`, `apps/nec-gui/src/solve.rs`), but a GUI sweep runs whatever range the user typed, and `SweepJob::solve_at` emits no geometry caveat at all. So a deck whose `FR` says 30 MHz, antenna at 0.634 m, swept 5–30 MHz in the GUI dips far below 0.1 λ with the low-ground caveat shown nowhere — the same miss class #399 fixes for `--hosts`, still live in the GUI. Path-inventory C4 row 3 read an unqualified "yes". | #400 — `SweepJob::geometry_caveats` evaluates the shared `hallen_geometry_caveats` producer at the sweep's worst-case frequency, the same choice the distributed path makes, and the caveats are sent when the job is prepared rather than when it finishes so a long streaming sweep does not withhold them until the end. `a_sweep_earns_the_caveats_its_range_deserves_not_the_fr_cards` uses a deck that is *clean* at its `FR` frequency and low across its swept range, so the two answers genuinely differ; `a_sweep_that_stays_high_earns_no_low_ground_caveat` is its mirror. Review then caught two more: the panel repeated the frequency-independent caveats the deck strip already shows, and the worst-case annotation had been hand-rolled in both consumers and already diverged — `validate::swept_low_ground_caveat` now owns it, and `the_sweep_panel_does_not_repeat_what_the_deck_strip_already_shows` pins the first. The message *send* remains unpinned (FND-034). Found by fable's diff review of #399 |
| FND-041 | 2026-08-24 | fixed | The worker discards `parse_result.warnings` in `crates/nec_worker/src/solve.rs` (`let deck = parse_result.deck;`), so deck-level parse caveats never reach a caller. Masked for the CLI, which parses the identical bytes and prints its own parse warnings before the `--hosts` branch — but not for anything driving `run_worker_stdio` directly, which is public API. The FND-026 field now exists to carry them. | found by fable's diff review of #398. **#418** — `parse_result.warnings` now travel with the result, ahead of the matrix-fill ones, since they describe the deck the rest was derived from. The `warnings` field FND-026 added was already on the wire, so nothing about the protocol changed. Sabotage-verified with an unrecognised-card deck, which is a *warning* rather than an error: the deck still solves and the user still needs to know a line was ignored. Sending them surfaced a second-order defect in the same PR: the CLI already prints local parse warnings once, so an M-point distributed sweep printed the same caveat M+1 times where a local run printed 1. The controller now prints each distinct worker line once, keyed on the rendered text so the same message from *different* workers is still shown separately — which is exactly what a mixed-version pool needs |
| FND-040 | 2026-08-24 | fixed | `fnec --exec gpu --hosts` against a worker with no GPU adapter is **silent**: the worker falls back to the CPU without a warning where the local CLI emits one, and although the fact does cross the wire in `exec_used`, the controller discards it under `..` and hardcodes `bench.exec` to `"ssh"`. So a user who asked for GPU cannot tell they did not get it, and a benchmark record says `ssh` regardless. Genuinely worker-only knowledge, and the strongest case for the FND-026 warnings field beyond stamps. | #410 — the controller destructures `exec_used` and warns when a GPU was requested and something else ran; `bench.exec` is `ssh-{exec_used}` rather than the hardcoded `ssh`, which named the transport and hid the execution path. Factored into `exec_fallback_warning` so it is reachable by a test rather than being a line in the result loop nothing can call — the FND-034 lesson, applied instead of relearned. Sabotage-verified in both directions: never warning fails, and warning on every run fails too. `exec_used` defaults to `cpu` for an old worker, so no fallback can be invented that did not happen |
| FND-039 | 2026-08-24 | fixed | `validate::diagnose` says nothing about an unrecognised `EX` type, so the GUI's keystroke-time caveat panel shows a deck as clean and it then fails at solve with `EX: unknown excitation type (I1=9, …)`. Pre-existing, and unchanged by #397 — but #397 makes it the *only* report for a deck that also has a degenerate fed segment, where the CLI used to say "source-risk" first. An `unsupported-excitation` warning in `diagnose` would close it; that is a new diagnostic, not an adoption. | found by fable's diff review of #397. **#423** — `validate::unrecognised_excitation_warnings`, folded into `diagnose` alongside the other solver-independent caveats, since an `EX` type nothing recognises is a fact about the deck and both solvers refuse it. **A warning, not an error, and that is the fix rather than a compromise**: the GUI's strip renders `diagnose`'s warnings and deliberately swallows its errors — a hard rejection being the action's job to report — so raising this to an error would have left the defect exactly as it was. Sabotage-verified in both directions: removing it from `diagnose` fails, and promoting it to an error fails too. Reproduced before and after at the GUI seam, which is where it was visible — `deck_warnings` returned `[]` and the solve then said `EX: unknown excitation type (I1=9, ...)`. Negative control sweeps types 0-5 from the canonical range rather than spot-checking, so a newly supported type cannot be silently reported as unrecognised |
| FND-038 | 2026-08-24 | fixed | A current-source-only (`EX 4`) deck now **errors** in the GUI and `fnec_py` where it previously returned `Ok` with a garbage impedance — the zero RHS solves, and the old unfiltered loop reported `V/I` from it. Erroring is the right direction, but the message is wrong. **Erratum:** this row first said the message blamed a missing *segment*; it did not — both frontends said "deck has no EX card", which is false for a deck whose only excitation is an `EX` card. The real problem is that neither frontend can price a current source (that needs the solved port voltage, which only the CLI's Hallén path computes). The worker got a named rejection in #396; these two should get the same shape. | #404 — `validate::unpriceable_feedpoint_error` is the shared reason, with the remedy left to the caller since it differs ("run without `--hosts`" vs "use the fnec CLI"). It collapses the worker's inline copy at the same time, so the third frontend never grew a third wording. Sabotage-verified: suppressing the named reason fails tests in `nec_solver`, `nec-gui` and `nec_worker` at once. Found by fable's diff review of #396; disclosed in v0.15.0's release notes as a known wart before being fixed |
| FND-037 | 2026-08-24 | fixed | `solve_mpie_session` picks its feedpoint with `!ex.kind().is_plane_wave()` (`apps/nec-cli/src/solve_session.rs:1146`), so it admits a type-4 current source and an unrecognised type as delta-gap feedpoints. Shielded today only by gates in a *different* function, which is the arrangement FND-031 existed to retire. Left out of #396 with the other two `validate` copies: the MPIE path has its own rejection rules and changing what it accepts is a behaviour change, not an adoption. | #414 — closed **at the move**, not after it. `solve_mpie_session` moved into `nec_solver::mpie_session` for the GUI's solver picker, and a shared function may not depend on what its callers happened to check first, so the lookup became `first_delta_gap_feedpoint` (types 0 and 5; excludes type 4 and unrecognised types). Sabotage-verified **independently of** the card guard: with `mpie_unsupported` intact, restoring the old `!is_plane_wave()` lookup still fails `an_unrecognised_excitation_is_refused_by_the_solve`, because an `EX 9` deck passes the guard — it names only plane waves, current sources and unstampable cards — and used to be driven with whatever voltage fields the card carried. **Reachable from a library caller, not from the CLI:** the CLI's excitation builder refuses an unknown `EX` type before solver dispatch, which is the shielding-by-another-function this row objected to; PR2's frontends are exactly the callers that would not have had it. The first sabotage pass found no such test and the gap was real; it was written before this row was closed. Found by fable's diff review of #396 |
| FND-036 | 2026-08-24 | fixed | Mixed multi-source decks are handled inconsistently and nobody has decided what they should mean. `solve_session.rs`'s "first CurrentSource" `find_map` ignores deck order relative to a type-0 card, and the worker silently omits a current source from the physics of a mixed `EX 4` + `EX 0` deck rather than rejecting it. Deliberately kept out of #396: the seam answers "which card is *a* feedpoint", not "what does a deck with several kinds of source mean", and conflating them would have made that PR a semantics change. | #411 — the maintainer decided (2026-08-26) to refuse rather than implement superposition: the numbers were never meaningful, so nothing is lost by declining to print them. `validate::mixed_excitation_error` names both offending cards, reached by all four frontends through the new `validate::pre_solve_error` aggregate — added rather than bolting a non-geometry refusal into a function called `geometry_error`, where nobody looking for "why was my deck refused" would find it. **Measured:** the EX 0 row reported 0.678 + j0.086 Ω against 74.243 + j13.900 for the same deck without the EX 4. Gated at the CLI entry point as well as in the unit, because dropping the check from the aggregate left every unit test green while the CLI printed the wrong number again. Found by fable's design review of the FND-031 seam, which refused to let it into that PR |
| FND-035 | 2026-08-24 | fixed | `validate::source_risk_geometry_error` applies **no** `EX` type filter, so a plane wave whose NTHETA/NPHI collide with a short fat segment's `(tag, index)` produces a spurious **hard rejection** of a valid receive deck — on every frontend, since it reaches `geometry_error` → `diagnose`. **Demonstrated:** a receive-only deck (`GW 1 3 …` radius 0.01, `EX 1 1 2`, no driven source at all) exits 1 with "unsupported source-risk geometry: EX on tiny segment tag 1 seg 2" — there is no source on that segment. `feedpoint_at_junction_warnings` (`validate.rs:377`) has the same shape with a plane-wave-only skip, so an unrecognised type counts as a feedpoint there. Both are held out of #396 because they change which decks are *rejected* or warned about, which wants its own negative controls. **Owns path-inventory C6 row 10.** | #397 — both `validate` sites now go through `nec_solver::feedpoints`, so only a **driven** source is checked. `a_plane_wave_does_not_trigger_a_source_risk_rejection` and `an_unrecognised_excitation_is_not_treated_as_a_junction_feedpoint`, each with a positive control on the same geometry so a check that simply stopped firing would fail; plus the end-to-end `a_receive_only_deck_is_not_refused_for_a_source_it_does_not_have` at the production entry point, since this changes which decks are *refused*. All three sabotage-verified. Fixture `corpus/receive-planewave-fat-segment.nec`. Found by fable's design review of the FND-031 seam |
| FND-034 | 2026-08-24 | fixed | The GUI's sweep stream closure is inline in `FnecGui::update`, so nothing pins what it sends — now **three** unpinned sends, after #400 added the geometry caveats. The closure captures no `self`, only `deck_text`, the sweep bounds and `output`, so extracting `async fn run_sweep_stream(..., output: impl Sink<Message>)` and driving it with a futures mpsc channel would pin all three at once — about thirty lines, not the refactor this row first implied. Deleting the `SweepCaveat` send on the mid-sweep-failure path — the exact behaviour a review blocked #395 on — fails no test: the regression test fabricates the message and pins only the reducer. The completion-path send is equally unpinned. Pinning either needs the closure extracted from `update`, which is a refactor rather than a test. | #401 — the body moved to `nec_gui::sweep_stream::run_sweep_stream`, which takes the output as a `Sink` and is driven in tests by a futures mpsc channel. Four tests assert the message *sequence*, not just presence: the geometry caveats must precede the first point (sending them at the end would still be "present" and would not do their job), and the mid-sweep-failure path must send **two** `SweepCaveats` rather than at least one — `any()` was satisfied by the pre-loop send even with the failure-path aggregate deleted, so that sabotage survived until the assertion counted. All three sends now fail when removed, and so do two reorderings. **Residual, stated rather than glossed:** the failure-path aggregate is pinned for presence and position but not content — the only deck that reaches that branch dies on point zero, so its aggregate is legitimately empty, and no frequency-dependent failure mode exists inside `solve_at` to build a better fixture from. The completion path's aggregate *is* content-pinned, which closes the realistic regression (right count, right order, wrong producer — that one passed the entire 171-test suite before). Found by fable's follow-up review of #395, which demonstrated the deletion against the full 163-test `nec-gui` suite; its cost was corrected in the #400 review |
| FND-033 | 2026-08-24 | fixed | A sweep that fails partway discards the points it already computed: `SweepComplete(Err)` sets `SweepPhase::Failed(String)`, which carries no points, and `sweep_view` renders the chart and table only for `Streaming`/`Done`. So partial results vanish at the moment of failure, and #395's negative-resistance caveat for them stands next to the error alone. Keeping the streamed points on the `Failed` phase would preserve the data and make the caveat's context visible. | found by fable's follow-up review of #395, which caught three comments asserting the opposite. **#428** — `SweepPhase::Failed` carries the points as well as the error, and the view renders them. A sweep that fails at point 400 of 500 has 399 real answers, and discarding them also left #395's negative-resistance caveat standing beside an error with nothing to describe. The status line says how far the run got. Negative control: a sweep that fails having computed nothing must not claim partial results, or 'keep the points' could be satisfied by inventing some |
| FND-032 | 2026-08-24 | fixed | `fnec_py`'s sweep re-creates the per-point warning spam the GUI aggregate was designed to prevent. `emit_warnings` dedups on exact message identity, and its own comment says that exists so "a sweep would otherwise repeat the same geometry caveat once per frequency point" — but the negative-resistance message embeds `Re Z = {z_re:.3}`, making it the first sweep-emitted warning whose text varies per frequency. A 500-point junctioned sweep through `sweep_deck_str` therefore raises ~500 distinct `UserWarning`s. The GUI solved this by aggregating; the bindings need the same treatment or a dedup key that ignores the value. | found by fable's diff review of #395; the new py tests cover `solve_deck_str` only. **#428** — the per-point sentence is held back during a sweep and one aggregate line is emitted, through `validate::swept_negative_resistance_caveat` hoisted out of the GUI so both use the same sentence. Recognising the per-point message needs a predicate rather than identity, because the text embeds `Re Z = {z_re:.3}` and every point differs; `is_negative_resistance_message` and the builder share one phrase constant so they cannot drift apart |
| FND-031 | 2026-08-24 | fixed | Resolving "which segment is the feedpoint" from a deck's `EX` cards existed in **eight** copies with five different filter policies: three loops in `nec_solver::excitation` (skip plane-wave/current-source, error on other types), the worker (type-0 only), the CLI's local path (skip plane-wave only), the GUI and `fnec_py` (**no filter at all**), and a sixth #395 added controller-side — whose first form matched the loosest policy and whose second still admitted `EX 5`, which the worker skipped. Same shape as FND-014/015/023/025/027: N copies, one diverged. | #396 — `ExcitationKind::feedpoint_role()` classifies the card; `nec_solver::feedpoints` and `first_delta_gap_feedpoint` answer the deck-level question. Eight sites adopted; the three RHS builders keep their `UnsupportedType` channel but `match` on the role with **no wildcard arm**, so a new excitation type breaks the build rather than defaulting. **Two copies were bugs, both demonstrated:** the worker drove a type-5 card as a delta gap and then refused to read the answer it had computed (`a_type_5_voltage_source_is_a_feedpoint_here_as_it_is_everywhere_else`, red on main), and the GUI/bindings read a plane wave's NTHETA/NPHI as a feedpoint tag and segment. Found by fable's review of #395; its design review then refuted the policy I proposed, which would have deleted the corpus-pinned `dipole-ex4-freesp-51seg` row. Three `validate`/MPIE sites stay open as FND-035 and path-inventory C6 rows 10-11 — they change which decks are *rejected* |
| FND-030 | 2026-08-24 | fixed | A `NaN` feedpoint resistance is reported with no caveat anywhere. `is_negative_resistance` deliberately excludes it — the negative-resistance sentence would read "has negative resistance (Re Z = NaN Ω)" and blame a junctioned geometry that is not the cause — but a `NaN` impedance is a non-converged solve and deserves its own diagnostic. #373 added divergence rejection for the GPU-resident path; the CPU paths have no equivalent. Pre-existing: the CLI's emission filter was already `< 0.0`, so this was silent before the check was shared too. | found by fable's diff review of #395, which caught that the shared gate had flipped the predicate to include `NaN`. **Route corrected and the input half closed in #417.** My first attempt to reach the `NaN` failed and I nearly recorded the row as unreachable; fable's review refuted that by finding the front door — `f64::from_str` accepts "NaN", "inf" and "-inf", and a deck is plain text, so `GW 1 21 0 0 NaN 0 0 5.0 0.001` printed a feedpoint row of `NaN NaN NaN NaN NaN NaN` at exit 0 with no warning. `NaN` fails every comparison, so `current.norm() > 1e-60` took the zero-current branch and `is_negative_resistance` (`z_re < 0.0`) stayed silent — the value defeated both checks by being unordered rather than by being wrong. `parse_f64` now rejects non-finite fields on **every** card, closing the whole input class in one place. **Still open** for the residual the parser cannot reach: a genuinely diverged *CPU* solve producing `NaN`, where #373 gave only the GPU path a guard. That route is unproven either way, and should not be called closed without a demonstration. **Residual closed by #425.** The remaining route was a diverged CPU solve producing `NaN` at the feedpoint, where #373 had guarded only the GPU path. `feedpoint_impedance` now returns `NonFiniteCurrent` for exactly that, at all six call sites, and says the solve did not converge rather than that the current is zero — the two were previously indistinguishable because `norm() > 1e-60` is false for `NaN`. Not demonstrated with a naturally diverged CPU solve, which I could not construct; the guard is verified directly by unit test instead, and the claim here is the narrow one that the reported-without-a-caveat path no longer exists |
| FND-029 | 2026-08-24 | fixed | The negative-resistance diagnosis recommended `--solver mpie` as a cross-check without asking whether the MPIE can take the deck. `nec_solver::validate` already has `mpie_compatible_deck` for exactly this, and `unsupported_topology_warning` obeys it with a committed negative-control test — but the CLI-private copy of this check did not, so a junctionless deck carrying an `LD` card and a negative resistance was pointed at a solver that rejects `LD` outright. Latent while the check was CLI-only; promoting it to a shared seam would have spread it to three more frontends. | #395 — gated on `mpie_compatible_deck`; `a_loaded_junctionless_deck_is_not_sent_to_a_solver_that_rejects_it`, mirroring the existing topology-warning negative control. Found by fable's design review before a line was written |
| FND-028 | 2026-08-24 | fixed | Eight local branches, 4–7 months stale (2026-04-25 … 2026-07-13), had survived every iteration close. `git branch -d` refuses all of them because the repo squash-merges, so ancestry says "unmerged" for work that shipped — which is exactly how a backlog like this accumulates unnoticed. Triaged one by one against `origin/main` content rather than ancestry: seven landed under a different commit (`docs/autopilot-operating-contract.md`, `docs/nec5-frontier.md`, the PH9-CHK-002 deferral note, the v0.12.0 tag, and #47/#55/#110 for the rest), and the eighth's only unlanded file, `apps/nec-cli/tests/hallen_fr_gpu_stub.rs`, was superseded by `apps/nec-cli/tests/hallen_fr_cpu_reference.rs` — its `compute_hallen_fr_*_stub` entry points no longer exist, though `GpuSegment` and `HallenFrGpuKernel` do. | #394 — deleted; tips recorded below so the deletion is reversible |
| FND-027 | 2026-08-24 | fixed | `fnec --hosts h.toml --ground-solver sommerfeld <GN2 deck>` silently ignored the flag: it is parsed in `main.rs` and consumed only by the local path, `run_distributed_solve` does not take it, and the worker derives its ground model from the deck alone — so the run returned the uncorrected reflection-coefficient impedance with no warning anywhere. Measured on `corpus/dipole-gn2-near-ground-51seg.nec`: 92.266 + j13.617 Ω without the PH9-CHK-006 correction against 95.524 + j12.166 Ω with it, a 3.26 Ω answer change for a flag the user passed explicitly. **The second member of FND-025's class** — the reviewer's point being that fixing one instance of "a flag the distributed path drops" leaves the class open. Swept for others: `--pulse-rhs` and `--sin-fallback-rel-max` only matter for bases the worker rejects loudly, `--vars`/`--sweep-config` are applied before the branch, and `--exec hybrid` degrades silently but is a performance preference, not a wrong answer. | #393 — rejected beside the FND-025 check, ahead of `WorkerPool` construction; `distributed_run_refuses_sommerfeld_before_contacting_any_host`, sabotage-verified (removing the check fails it in 10.07 s, having dialled the host) |
| FND-026 | 2026-08-24 | fixed | The worker builds its stamps through the seam but drops `stamps.warnings` on the floor — `TaskResult::Ok` has no warnings channel, so a malformed `LD`/`TL`/`NT` in a distributed run is silent where every other frontend reports it. This is the residue of FND-015 that #393 did not close. The fix is `#[serde(default)] warnings: Vec<String>` on `TaskResult::Ok`, wire-compatible in both directions. **It does not share a fix with FND-014**, as this row first claimed: #395 closed FND-014 controller-side precisely because a worker-side warning goes silent against an older worker, whereas these stamp warnings are facts only the worker holds and must travel on the wire. | #398 — `#[serde(default)] warnings: Vec<String>` on `TaskResult::Ok`, filled from `stamps.warnings`, printed by the controller. Wire compatibility gated in **both** directions: a legacy line without the field, and a line carrying an unknown extra field (which also stops anyone adding `deny_unknown_fields` — sabotage-verified). Plus `a_malformed_card_the_worker_skips_is_reported_not_swallowed`, and `a_skipped_card_warning_survives_the_wire_to_a_real_worker`, which dispatches to `fnec worker --stdio` as an actual subprocess — the serde round-trip cannot catch a worker binary that never fills the field. `warnings_survive_a_cache_hit` pins the container contract only; `ResultCache` is not wired into dispatch, so there is no warm path for it to diverge on. Found by fable's diff review of #393; framing corrected in #395; `docs/project/path-inventory.md` C5 row 4 |
| FND-025 | 2026-08-24 | fixed | `fnec --hosts h.toml --loads-config foo.toml deck.nec` silently discarded the Laplace loads for the whole run, CPU included: they are parsed in `main.rs` but `run_distributed_solve` takes no Laplace parameter and the worker protocol carries no field for them, so the controller returned the *unloaded* impedance for a deck the user had explicitly loaded. FND-023's exact signature one layer up. | #393 — rejected before `WorkerPool` construction, next to the FND-013 geometry check; `distributed_run_refuses_laplace_loads_before_contacting_any_host`, sabotage-verified (removing the check fails it in 10.05 s, i.e. having dialled the host) |
| FND-024 | 2026-08-24 | fixed | `cargo fmt --all` at the workspace root does not reach `bindings/fnec_py` — it is excluded from the workspace and carries its own lockfile, so the root fmt gate passes on an unformatted bindings crate. Only the CI `python bindings` job catches it, which is how #393 went red on a two-line import wrap after every local gate had passed. Measured with a planted misformat: root `cargo fmt --all --check` exits 0 on it, while `cargo fmt --all --check` **run from `bindings/fnec_py`** exits 1 — the bindings tree pulls the workspace crates in as path dependencies, so that one invocation covers both trees and the root one covers only its own. Same blind spot for clippy, plus `cargo clippy` there needs `PYO3_USE_ABI3_FORWARD_COMPATIBILITY=1` on this machine (Python 3.14 exceeds pyo3 0.23.5's supported maximum). | found while pushing #393; the fix is a local all-gates script that runs both trees. **#416** — `scripts/check-all.sh` runs fmt, both clippy passes, the tests and all five doc checkers across both trees, reporting each with its exit code. The fmt step runs **from** `bindings/fnec_py`, which is the asymmetry that makes one invocation enough: the bindings crate pulls the workspace crates in as *path* dependencies, so a run there sees both trees while a root run sees only its own. Sabotage-verified in three classes — a misformat in the bindings (the exact blind spot, with the root gate's exit 0 captured beside the script's failure as the control), a clippy lint in the workspace, and a duplicated changelog heading. CI already caught all of this; what was missing was catching it *before* the push |
| FND-023 | 2026-08-24 | fixed | **`--exec gpu` silently discards host-side matrix stamps.** `maybe_gpu_resident_hallen` re-fills and solves on the device from raw segment inputs, so every host stamp is dropped when it succeeds — and its guard declines only on `Card::Ld` or `Card::Tl`, omitting `Card::Nt`, while taking no Laplace-loads parameter at all. Demonstrated on hardware: `corpus/dipole-nt-tl-equiv-freesp-51seg.nec` gives 70.633 + j14.009 Ω on `--exec cpu` and **74.234 + j13.898** on `--exec gpu` (the plain-dipole answer, NT dropped); worse, `--exec gpu --loads-config examples/laplace-load-rlc.toml` on a plain dipole gives **74.234 + j13.898** where CPU gives **442.655 − j971.944** — the load the user explicitly passed is discarded, a 6× error. Refutes the framing that the CLI is the correct baseline for FND-015. The worker's sibling gate is value-based and got this half right, which is the argument against hand-listed card types. | #393 — both GPU gates now key on `DeckStamps::is_identity()` instead of a hand-listed card set, and the CLI declines the device path when Laplace loads are present. Verified on hardware (AMD RADV RENOIR): NT deck and `--loads-config` now match `--exec cpu` exactly, while a deck that stamps nothing still reaches the device — plain dipole 74.234166 on `--exec gpu` against 74.242874 on `--exec cpu`, the f32 signature that proves the gate declines rather than the adapter being absent. That 74.234166 is precisely the answer the NT deck returned before the fix |
| FND-022 | 2026-08-24 | fixed | `check-path-inventory.py` only validated backticked names of 15+ characters, so the invented symbol `solve_task` sat in the path inventory and the ledger through two reviews without the gate noticing. Length was never what makes a name checkable — containing an underscore is. Fixing that exposed two more ways the check confirmed whatever it was given: it substring-matched, so a truncated name resolved to the real one it prefixes; and it searched `docs/` and `scripts/`, so a name resolved to its own mention in the file under check, or to the examples in the checker's own comment. | #391 — sabotage-verified against both `solve_task` and a truncated `build_nt_stamp` |
| FND-021 | 2026-08-24 | fixed | `build_hallen_rhs`'s non-`UnsupportedType` excitation errors map to `SolveError::ParseError` in the worker (`nec_worker/src/solve.rs`), so an `EX` referencing a missing segment crosses the wire labelled `parse_error` and sends the user to look at their deck's syntax. Same mislabel class the FND-013 fix avoided for geometry. Pre-existing. | #410 — mapped to `UnsupportedConfig`, not a new `ErrorCode` variant: that enum is serialised, so adding a variant breaks an older controller's deserialisation outright, unlike FND-026's additive field. Gated with a negative control, which caught my own wrong assumption — free text parses cleanly and fails later as `GeometryError("deck contains no GW cards")`, so a real parse failure needs a recognised card carrying an unreadable field |
| FND-020 | 2026-08-24 | fixed | The distributed path collects only half of concern C1: it rejects the unsupported geometry class but emits only the deferred-ground and unsupported-topology caveats, not `low_finite_ground_warning` or `feedpoint_at_junction_warnings`. A dipole at 0.03 λ over `GN 2` run via `--hosts` returns numbers with no low-ground caveat where every other frontend warns. | #399 — `distributed_pre_solve_caveats` emits every pre-solve caveat the local path does, controller-side, since it holds the deck, geometry, ground and frequencies before dispatch. Both paths call one producer, `validate::hallen_geometry_caveats`, so a caveat added there reaches both by construction — demonstrated with a probe caveat that appeared on the local and distributed routes of the real binary with no call site touched. (The first gate compared against a hand-copied list of the same three calls and would have stayed green if a *fourth* were added — the failure it claimed to prevent; caught in review.) Gated on `SolverMode::Hallen` like its sibling, since `--hosts --solver mpie` is reachable while FND-018 is open and the topology caveat would otherwise tell such a user to re-run with the solver they are already using. The frequency-dependent low-ground check reports at the worst case (the lowest swept frequency) with a count when only some points trip — sabotage-verified with a *descending* sweep, since an ascending one cannot tell "worst case" from "first" |
| FND-019 | 2026-08-24 | fixed | `WorkerSolverConfig::ground_model` is sent as `"none"` by the controller and never read by the worker's solve, which derives the ground from the deck (`ground_model_from_deck`). It does feed the result-cache key, so it is not wholly dead — but a protocol field that looks like it selects a ground model and does not is the same trap that produced FND-013. | found by fable's review of the FND-013 design. **#427** — refused rather than removed or quietly kept. The field reads as though it selects a ground model; the worker takes ground from the deck's `GN` card, which is authoritative and is what the local solve uses. A controller sending anything but `"none"` is now told, instead of having its choice discarded — the FND-013 trap is a field that looks like a control and is ignored. It stays on the wire because removing it breaks a protocol every controller already satisfies, and it feeds the result-cache key. Kept, documented truthfully, and checked. Negative control: `"none"` is accepted, so the ordinary distributed path is unaffected |
| FND-018 | 2026-08-24 | fixed | `fnec --hosts h.toml --solver mpie deck.nec` connects to every host (5 s SSH timeout each) before every task fails `UnsupportedConfig` — the worker accepts only `basis == "hallen"`. The basis should be rejected controller-side before any SSH spawn, next to the FND-013 geometry check. | found by fable's review of the FND-013 design. **#427** — refused at the same site as the `--loads-config` and `--ground-solver` pre-checks (FND-025/027), ahead of `WorkerPool` construction, because the pool spawns an SSH process per host the moment it is built. Proven the way its siblings are — the host name must not appear in stderr — **not** by timing: an unresolvable name fails DNS in milliseconds, so a timing assertion would have measured nothing. I initially claimed a saving of ~5 s per host without measuring it; the honest statement is that the answer is knowable before the first connection, and the delay saved depends on a host that resolves and does not answer. Negative control: a distributed Hallén run still reaches the hosts |
| FND-017 | 2026-08-24 | fixed | Six `nec-cli` integration-test files wrote uniquely-named decks to the system temp directory and never removed them, while the other eighteen did. One session of repeated `cargo test --workspace` runs left **437** stray `fnec-*` files in `/tmp` — enough to break the sandbox this agent's shell runs in, which is how it was found. Now a shared `common::TempDeck` RAII guard that also survives a panicking test, which the pre-existing trailing-`remove_file` convention does not. Residual, bounded and deliberate: 6 GUI-crate fixtures use fixed names, so they overwrite rather than accumulate. | #389 — a full `cargo test --workspace` now leaves 6 fixed-name files, down from ~437 unique ones |
| FND-016 | 2026-08-24 | fixed | `apps/nec-cli/Cargo.toml:21` declares `nec_project` as a dependency that no CLI source file imports — the binary links and the SBOM carries a crate it never uses. Either wire it (which is what FND-006 asks for) or drop the declaration. | found while correcting FND-006; `cargo` would catch it under the `unused_crate_dependencies` lint. **#426** — wired rather than removed. `nec_project` was a dependency no source file imported *because* GAP-015's entry points were never written, so the unused dependency and the overstated roadmap row were one omission with two symptoms. `project_cmd.rs` imports it; the SBOM now carries a crate the binary uses |
| FND-015 | 2026-08-23 | fixed | `build_nt_stamps` is called **only** from `apps/nec-cli/src/solve_session.rs`, so `NT` cards are ignored outright by the GUI, the Python bindings and the worker. **Demonstrated: the same deck gives different impedances per frontend** — `corpus/dipole-nt-tl-equiv-freesp-51seg.nec` solves to 70.633 + j14.009 Ω on the CLI (NT stamped) and 74.243 + j13.900 Ω via `fnec_py` (NT ignored — identical to the plain dipole), 3.6 Ω / 5 % apart. The worker separately discards `_load_warnings` / `_tl_warnings`. A malformed `NT` that the CLI reports ("NT card has 8 fields; expected 10") is silent everywhere else. | #393 — `nec_solver::build_deck_stamps` is now the single seam for LD/TL/NT across all six assembly sites; `fnec_py` on the NT deck matches the CLI-produced corpus reference. **Scoped to the stamping half.** The worker still discards `stamps.warnings` because its result type has no warnings channel; that residue is FND-026 |
| FND-014 | 2026-08-23 | fixed | The negative-resistance tripwire is CLI-only. The check is post-solve, so adopting `validate::diagnose` in #369/#370 did not carry it. **Demonstrated:** a 3-segment 40 m wire returns `Re(Z) = -162.547 Ω` — physically impossible for a passive antenna — where the CLI warns and `fnec_py` raises **zero** warnings. Positive control: the same build *does* raise a `UserWarning` for a low-antenna-over-finite-ground deck, so the caveat channel works and this one simply is not on it. | #395 — `validate::negative_resistance_warning` is now the shared seam, called by all four frontends. One sabotage (the predicate never warns) takes down five independent gates: `nec_solver` unit tests, the CLI's `junction_feedpoint` suite, the controller-side distributed mapping, the GUI's `solve` tests, and `fnec_py` pytest. Corpus fixture `corpus/inverted-v-negative-r-freesp.nec` |
| FND-013 | 2026-08-23 | fixed | The distributed path skipped pre-solve validation at **both** ends: `--hosts` returned from `main()` before the validation block, and `nec_worker::solve_deck_at_frequency_with_exec` went from `build_geometry` straight to the solve. A deck the CLI refuses locally was dispatched to every worker and solved — demonstrated: the worker returned 49.53 − j173.05 Ω for crossing wires. | #390 — one shared check hoisted **above** the `--hosts` branch (placement is load-bearing: `WorkerPool` spawns SSH on construction) plus an independent worker-side guard, since the worker is a separately installed binary a controller cannot speak for |
| FND-012 | 2026-08-23 | fixed | `gpu_kernels` CPU-emulation scaffold still exists, so a path can report CPU time under a GPU-sounding name. Roadmap: "retire or realize". | owner: unassigned; tracked in `docs/roadmap.md` Phase 6. **#430** — neither retired nor realized, because both options the requirement offered would have made it worse: retiring deletes the parity baseline the wgpu shaders are checked against, and realizing it as a GPU path deletes the point of having a CPU reference. The emulation *behaviour* went in #365; what remained was the **name**, which was the last true part of "a path can report CPU time under a GPU-sounding name". `nec_accel::gpu_kernels` is `kernel_reference` and `HallenFrGpuKernel` is `HallenFrReferenceKernel`. `GpuSegment` and `GpuFarFieldPoint` keep theirs deliberately — those genuinely are the layouts the shaders consume, and renaming them would trade one inaccuracy for another. The 2026-06 requirement text is left as written rather than edited to match; `docs/ph7-chk-001-gpu-stub-retirement.md` records why |
| FND-011 | 2026-08-23 | fixed | `--exec gpu` is not dispatched through the SSH worker pool, so GPU-capable remote nodes never solve on their GPU. | owner: unassigned; tracked in `docs/roadmap.md` Phase 6. **Already true when re-checked, and now covered.** PH7-CHK-004 wired `--exec gpu` through to the workers, and `worker_gpu_exec.rs` exercises a mixed pool — so the finding described code that had since changed. What kept it standing was that the controller-side derivation lived inside `run_distributed_solve`, which no test can call, so neither the claim nor its refutation was checkable. #430 factors it into `worker_exec_preference` and asserts both directions: a GPU run asks its workers for the GPU, and nothing else does — `Hybrid` splits work on the controller, which says nothing about what a remote node should do |
| FND-010 | 2026-08-23 | fixed | No ROCm or SYCL backend validation beyond the wgpu Vulkan path, and no dated "not yet" recorded either. | owner: unassigned; tracked in `docs/roadmap.md` Phase 6. **#430** — the finding's own words were "and no dated 'not yet' recorded either", so recording it *is* the fix; validating ROCm and SYCL needs hardware and is a separate item. `docs/gpu-arch.md` now carries a dated backend-validation section saying plainly that only the wgpu Vulkan backend has been run on real hardware, and only on an integrated adapter. The previous state was the worse one: a reader could infer from "via wgpu" that the others had been tried, because nothing said otherwise. wgpu makes them reachable; reachable is not tested |
| FND-009 | 2026-08-23 | fixed | The GPU-resident dense solve never beats the CPU at any tested size (0.04×–0.48×) and is declined by the accuracy gate at N=512. Cause is structural: the LU dispatches one workgroup, so it runs on a single compute unit and more GPU hardware cannot help. Recommendation to treat it as not-recommended is recorded but not acted on. | measured in #384; `docs/ph7-chk-003-gpu-resident-solve.md` § Performance. **#430** — the recommendation is acted on where a user can see it. PH7-CHK-003 measured 0.04x-0.48x and identified the cause as structural (the LU dispatches one workgroup, so it runs on a single compute unit and more GPU cannot help), then recommended treating the path as not-recommended — in a design document, which nobody running the flag reads. `--exec gpu` now warns at the point the GPU-resident solve is actually taken. A warning rather than a removal, because the Z-fill and far-field kernels on the same flag earn their place decisively (100-290x, 56-234x): it is the dense solve alone that loses, and saying *which part* is the useful thing |
| FND-008 | 2026-08-23 | deferred | Real-hardware GPU benchmark evidence exists only for an **integrated** adapter (RADV RENOIR). The roadmap asks for at least one **discrete** GPU; that is still unmet, and the PH7-CHK-003 results were previously headed "real discrete GPU". | heading corrected in #384; roadmap item marked partial. **owner: maintainer (hardware-blocked)**; tracked in `docs/roadmap.md` Phase 6 and `docs/gpu-arch.md` § Backend validation status. Deferred rather than fixed for a reason no amount of work here changes: the finding asks for benchmark evidence from a **discrete** GPU, and this development machine has an integrated adapter only (RADV RENOIR). Producing the number needs the hardware, not the code. What *is* done: the misleading heading was corrected in #384, the roadmap item is marked partial, and `docs/gpu-arch.md` now carries a dated statement of which backends have actually been run — so the gap is visible rather than inferred from silence. Re-open with a measurement when a discrete adapter is available |
| FND-007 | 2026-08-23 | fixed | The GUI is Hallén-only: no solver picker. Decks needing `--solver mpie` are warned about and told to use the CLI instead of being solvable in place. | owner: unassigned; surfaced by #369, #381. **#415** — the GUI has a two-entry solver picker (Hallén / MPIE) that routes through the single shared `solve_currents` step, so Solve, Sweep, Currents and Pattern all follow it; a picker that changed one tab would be FND-038 one solver over. The GUI reproduces the CLI's pinned MPIE digits (74.437414 + j41.753720; Y-junction 63.673674 - j322.199211), sabotage-verified: forcing the Hallén branch fails all three parity tests. Caveats are now solver-aware — a T/Y deck on the MPIE earns no Hallén topology caveat, and none of them quote a CLI flag at a user who has a picker (`validate::SolverContext` carries the remedy, the `unpriceable_feedpoint_error` precedent). This also retired the CLI's per-solver negative-resistance arm into the shared producer. Deliberately excluded: the experimental pulse/continuity/sinusoidal modes, which are known-inaccurate for thin-wire antennas. Bindings not adopted — FND-055 |
| FND-006 | 2026-08-23 | fixed | Roadmap `GAP-015` is marked Done citing only `nec_project`'s library functions and their round-trip tests, but its own acceptance criterion names "explicit CLI/API entry points", which do not exist. The library half is delivered; the frontend half the criterion asks for is not. | roadmap row corrected from Done to Partial on 2026-08-24; dispositioned in #377, `docs/dev/reviews/review-260719.md` § Dispositions. Stays open: the CLI/API entry points the criterion names still do not exist. **#426** — the missing half is delivered rather than the claim re-worded: `fnec project convert <in> [out]` is the CLI entry point the criterion names, with an end-to-end round-trip test through the **binary**, not the library. GAP-015 is Done for real now, and its roadmap row names both halves and the test covering each — the shape that would have stopped it being marked Done from one half the first time |
| FND-005 | 2026-08-23 | deferred | Corpus per-case provenance records the workspace version *under development* at the commit, which is not necessarily a released build, and does not distinguish a case being added from its values being regenerated. | owner: developer; acceptable limit stated in `corpus/reference-results.json` `provenance_note` and enforced fresh by CI |
| FND-004 | 2026-08-23 | deferred | DCIM Phase 3 (Rust port of the validated Python prototype) cannot proceed: nec2c / xnec2c / EZNEC / 4nec2 are unavailable on this machine, so new geometries cannot be gated against an external oracle. | owner: developer (restoring tooling); `docs/sommerfeld-level2-scope.md` |
| FND-003 | 2026-08-23 | rejected | Three extension traits (`DeckPostProcessor`, `ResultFilter`, `ReportSection`) have no production consumers. | Deliberate plugin-API surface per `docs/plugin-api-design.md`. Kept, but labelled "future-facing API, unused today" rather than "consumed" — the review's own refutation was wrong, since `render_text_report_with_sections` has no production caller either. #377 |
| FND-002 | 2026-08-23 | fixed | `⚠` (U+26A0) has no glyph in iced's default font and rendered as a tofu box in every GUI caveat — pre-existing, and propagated to the new caveats strip. Only a visual check could see it; the tests assert on the text, which was always correct. | #383 |
| FND-001 | 2026-08-23 | fixed | `exec_modes` drop-in alias paths were keyed on `(name, nanoseconds)`, but six matrix tests loop over one shared alias-name list in parallel — two threads collide and `fs::copy` fails with `ETXTBSY`. Took down a release-branch coverage run. | #379 |

## Recovered branch tips (FND-028)

Deleting a local branch does not delete its commits. These eight tips were
recorded before deletion so any of them can be restored with
`git checkout -b <name> <sha>` for as long as the objects survive gc:

| Tip | Last commit | Branch |
|:----|:------------|:-------|
| `a5141ca` | 2026-04-28 | `docs/autopilot-operating-contract-export` |
| `54800ff` | 2026-04-26 | `feat/corpus-validation-expansion` |
| `662081e` | 2026-04-26 | `feat/gpu-kernel-acceleration` |
| `b6055cc` | 2026-04-25 | `feat/loaded-element-experimental-path` |
| `1245f75` | 2026-05-04 | `feat/ph6-chk-002-nec5-frontier` |
| `feed0c4` | 2026-07-08 | `feat/ph9-degree3-junction` |
| `d663690` | 2026-04-25 | `pr-54` |
| `870dc0e` | 2026-07-13 | `release/v0.12.0` |
