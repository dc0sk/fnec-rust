#!/usr/bin/env bash
set -euo pipefail

cargo fmt --all -- --check
# Warnings-as-errors, matching the CI clippy gate so a warning can't slip past
# a commit here and only surface in CI.
cargo clippy --workspace --all-targets -- -D warnings
